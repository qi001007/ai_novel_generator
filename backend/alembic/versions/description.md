# alembic/versions/ —— 一条迁移一个 revision

**规矩**：结构迁移与数据迁移分条写，别混在同一个 revision 里。
**数据迁移的红线**（踩过一次）：任何会重写主人已有章号 / 弧范围 / 删目录行的自动迁移，
必须先出逐行报告 + 先落快照，**由他在界面上按键触发**（D-29 末段、D-32）。
第二十八批有一条自动补洞的 `densify` 迁移就是这么被自动审查驳回并整条撤回的
（库里一个字没改），那次撤回是这条规矩的出处。
