# AI 网文生成工作台 PRD

版本：v0.4  
状态：自用已确认，随决策演进。设计理由与废止口径见 docs/DECISIONS.md（按 D-xx／X-xx 引用）  
定位：单机、自用优先的 AI 长篇连载生成与审稿工作台  

## 1. 产品判断

这不是一个面向大众的 AI 写作 SaaS，而是先解决作者本人的核心问题：长篇连载最容易崩的不是“生成不出一章”，而是设定漂移、角色失控行为、伏笔断裂、节奏崩坏，以及人工检查成本过高。

因此产品重心不是“全自动成书”，而是“分层规划 + AI 生成 + 机械校验 + AI 七维自检 + 人工终审 + 已确认事实回写”。系统必须让作者能稳定推进几百章，而不是让作者陷入重复整理设定和上下文的时间黑洞。

## 2. 用户与场景

- 主要用户：作者本人。
- 维护作品列表，进入某个作品后推进章节。
- 每天推进 1-3 章，每章 2000-4000 字。
- 开新卷、调整主线、回收伏笔时检查全局一致性。
- 作者给出新剧情反馈，希望 D 层立即调整，C 层局部调整，B 层选择性调整，A 层只做低频校准。
- 人工终审时直接编辑正文，而不是只能在编辑器外改完再粘贴回来。

不做：多用户协作、公开发布平台、全自动批量成书、复杂工作流编辑器。

## 3. 目标与非目标

### v1 目标

1. 提供作品列表；每个作品拥有独立的规划、设定、章节、摘要和审稿数据。
2. 完成四层规划：A 全书蓝图、B 目录章节序列、C 10-30 章剧情弧、D 单章简报。
3. 完成核心流水线：章纲 → 正文生成 → 机械校验 → AI 七维自检 → 人工终审 → 确认事实落库。
4. 使用 SQLite 持久化作品、规划、设定、角色、摘要、AI 结果与人工决定。
5. 支持按任务类型配置模型。实测四个：`draft`、`review`、`summary`、`chat`（`llm.py:44-47`）；`outline` 与 `fact_extract` 未实现。
6. 提供 Codex/VSCode 风格工作台：左侧规划与设定双根树、中间对话 Agent、右侧写作编辑器，支持正文直接编辑。
7. 每章保留可追溯记录：模型输入摘要、生成版本、校验结果、终审决定。

### v1 非目标

1. 不做多用户协作和权限系统。
2. 不做向量 RAG。上它的唯一合法前置：出现过一次可复现的「该给的章节没给」且确认是排序错而非未采集（D-12）。
3. 不做自动发布到小说平台。
4. 不做插件系统。
5. 不做 DAG / 工作流编辑器。
6. 不做全自动级联改写 A/B/C/D 规划。

## 4. 四层规划架构

规划必须分层，否则长篇会同时承担“全局约束不足”和“局部上下文爆炸”两个问题。

| 层级 | 名称 | 粒度 | 变更频率 | 作用 |
|---|---|---|---|---|
| A | 全书蓝图 | 最长期，全书主线、终局、核心冲突、主题 | 低 | 防止越写越散，是最高约束。 |
| B | 目录规划 | 章节顺序、章节名、每章承担的剧情功能 | 中 | 让章节序列在大背景下有起伏、反转和钩子。 |
| C | 剧情弧窗口 | 10-30 章小情节，主角完成一件事或下一次小副本 | 高 | 兼顾具体情节和连贯性，通常维护当前 + 后续 10-15 章。 |
| D | 单章简报 | 本章目标、事件、POV、出场人物、冲突、钩子 | 每章 | 直接服务正文生成。 |

规则：

- 四层规划的**唯一写入口**是文档层（4.4）。
- D 层必须可独立编辑。
- C 层是生成正文时的主要上下文，不做无限扩展。
- B 层是章节顺序和剧情功能的索引，可以滞后于 C/D 层，但不能长期矛盾。
- A 层是最稳定的低频约束，不应被单章反馈频繁重写。

### 4.1 C 层：弧是存储，滑动窗口是派生视图

主人 2026-09-02 批注：C 层作为滑动窗口，内容实时变更，究竟该用一个从头走到尾的 MD 承担，
还是定死 1~30 / 31~60 这种固定窗口，还是有更好的设计。三个候选的取舍：

| 方案 | 做法 | 结论 |
|---|---|---|
| a 单文件滑块 | 一份 `window.md` 随进度从头改到尾 | 否决。窗口即存储：推进一章就覆盖上一弧的计划，无法回查"写第 47 章时原本打算怎么走"；每次推进都改同一文件，提案 diff 与并发都难受 |
| b 固定章窗 | `1-30 / 31-60` 定死 | 否决。弧的长短是内容决定的（7 章收束或 40 章才收），固定边界会把一个弧劈成两半；窗口大小还被章号写死，与 token 预算绑在一起调不动 |
| c 弧对象 + 派生窗口 | 弧是存储单位，不定长、带状态；窗口在生成时现算 | **采用** |

采用 c 之后的规则：

- C 层存储 = 弧（`arc_plan`）：起止章、目标、冲突、收束、状态（未开始 / 进行中 / 已收束）。
  弧不随进度删除，写完只改状态，保留可回查。
- **滑动窗口不落盘**。每次生成第 N 章时现算，固定组成与顺序：
  `A 蓝图 → B 目录邻域（N±3 章） → C 当前弧（跨界时附上一弧收束摘要） → D 本章简报
  → 事实库（出场人物当前状态 / 未回收伏笔 / 上一章结尾）`。
  窗口宽度只受 token 预算约束：改预算不改存储，这一层就是成本与"不忘关键内容"的调节旋钮。
- 不变量：**未回收伏笔与出场人物状态永远在窗口内**，不靠窗口位置命中。这是长篇不失忆的底线。
- 窗口外的事实以**分层摘要塔**注入（章摘要 → 弧收束摘要 → 卷摘要），不是逐章原文。
  章摘要已有 `chapter_summary`，弧级与卷级摘要排在 Phase 3。

### 4.2 文件组织：树按章归组，DB 仍是唯一真源

主人的方案是"四层都在一个文件夹里管理，正文与 D 层规划同处一章目录"。方向采纳，
但必须先说清一个事实，避免假装照做：

- 现状 `blueprint.md` / `toc.md` / `arcs.md` / `chapters/NNNN/{draft.md,brief.md}` **不是磁盘文件**，
  是 `documents.py` 从 `planning_blueprint` / `toc_entry` / `arc_plan` / `chapter_brief`
  四张表现渲染的**投影**，写回时解析回列。真源是 SQLite，磁盘上并不存在这个文件夹。
- 所以"按章归组"落在**树的展示结构**上，零数据迁移：

```text
规划/
  blueprint.md      A 层全本蓝图（1 份）
  toc.md            B 层目录（列表视图，见 4.3）
  arcs.md           C 层弧序列
  chapters/         按章归组（主人要的"一章一个抽屉"）
    0042/
      draft.md      正文投影
      brief.md      D 层简报投影
    0043/
      draft.md
      brief.md
```

- 一章一目录、正文与简报同组，后端路径已收口；旧路径继续兼容，不动表结构。
- 旧路径 `chapters/{N}`（正文）与 `briefs/0042.md`（简报）继续可解析，不断链。
- 真磁盘文件（可 git、可手改、可备份）列为 P2 的**单向导出镜像** `exports/<书名>/…`，
  读仍以 DB 为准；双向同步要引入冲突解决，v1 不做。

### 4.3 B 层目录：以可搜索列表呈现，MD 退为源码视图

主人 2026-09-02 批注：目录只有 MD 纯文本形态，不方便搜索管理，也不好看；要做成列表并支持搜索。

- B 层的数据形状天然是表（章号 / 章名 / 剧情功能 / 备注），适合列表；A/C/D 仍是文档。
  这是 3.0「一切规划皆文档」的**唯一有意例外**：存储仍是同一张 `toc_entry` 表，改的只是视图。
- 默认视图 = 列表：可搜索、可按章号跳转、行内改章名与剧情功能、显示该章正文状态。
  「源码」开关切回 `toc.md` 文本视图。两种视图共用同一份数据与同一套锁规则（章号是主键）。
- 搜索 v1 做**本地全字过滤**（章名 + 剧情功能 + 备注，命中高亮、自动定位）；
  跨作品全文检索排 P2。

### 4.4 四层规划的写入口：文档层（B 路）

主人 2026-09-03 裁定，全量理由与新旧口径见 `DECISIONS.md` D-01 / D-02 / D-03。

- **唯一写入口** = `PUT /api/novels/{id}/files/{path}`。人改文档、AI 提案、新建章节，三种来源全走这一条。
- 该入口自带四道保护：小节标题与主键锁定（422）、`actor=ai` 字段白名单、`base_revision` 乐观并发（409）、
  提案稳定化（未改动小节逐字节不变）。
- `routers/planning.py` 的 POST/PUT 端点降级退役，只读 GET 按需保留。**禁止再开第二条写通路。**
- **DB 仍是唯一真源**：`.md` 是每次从列现渲染的投影，库里不存文档文本。选 B 路选的是「写入口」，
  不是「把真源搬进文件」。
- 为什么选文档入口：与已批准的 UI 范式一致（一切规划皆文档）；AI 改规划走「提案 + diff + 人点应用」时，
  文档比字段级 PATCH 更容易让人读懂改了什么。
- 代价（已知并接受）：`/planning/*` 那 9 个端点退役、`store/workbench.ts` 的新建章节改走文件层。
  排期为 S3，且**必须早于 S2**——Agent 的管理规划命令若绑在两条通路上，等于把双真源自动化放大。

## 5. 剧情反馈系统

作者反馈不应直接覆盖全部规划层，而应先进入影响分析，再由作者确认要改哪些层。

```text
作者反馈
  → 影响分析：判断影响 D / C / B / A
  → 生成建议修改
  → 作者确认
  → 更新对应层
  → 记录规划版本
```

影响规则：

- 单章节奏、视角、事件微调：优先影响 D。
- 小情节走向、后续 10-15 章节奏、角色目标变化：影响 C，可能连带 D。
- 章节顺序、章节名、重要反转位置变化：影响 B，可能连带 C/D。
- 主线终局、核心冲突、主题变化：影响 A，必须人工明确确认。

v1 不做自动级联改写。系统先给出建议和影响范围，作者确认后才更新。这样能节约 token，也能避免作者只改了一个细节却把全书蓝图改乱。

## 6. 核心流水线

```text
D 章节简报
  → 正文生成
  → 机械校验：字数、禁用词、黑名单、必要字段
  → AI 七维自检：一致性 / 角色行为 / 节奏 / 连贯 / 伏笔 / 钩子 / 文风
  → 人工终审：通过 / 编辑后通过 / 打回重生成
  → 事实落库：章摘要、角色状态、关系变化、伏笔状态
```

规则：

- 章节状态只前进，唯一显式回退是“打回重生成”。
- 只有“确认通过”后的内容才会更新事实库。
- AI 自检做完整七维，但每条结论必须引用原文证据；没有证据的结论只算提示，不算评分。
- 人工终审允许直接编辑正文；编辑后保存为新版本。
- 每章显示 token 用量与成本估算。

### 6.1 上下文注入：一个构造器，一份可查清单

主人的要求（2026-09-02 批注）：调成写作模式让 AI 写某章时，要能在终端或某处看到
**本次续写实际注入了哪些上下文**；觉得多余就让机制删，觉得缺就让机制加。

先记录一个必须治本的真实缺陷：系统里有两套互不相通的上下文通路。

| 通路 | 代码 | 实际注入 |
|---|---|---|
| 对话 Agent | `services/chat.py` → `build_context()` | 全量：预算裁剪、相关度排序、@引用 |
| 正文生成 | `routers/chapters.py` → `build_draft_user_prompt()` | 只有 novel / blueprint / arc / brief 四个对象 |

后果：`/generate` 写正文时**没有伏笔、没有人物当前状态、没有上一章结尾**；
而且它取蓝图用 `version` 升序第一条（最旧版本），与对话侧"取最新 active"相互矛盾。
同一个"上下文注入"存在两条实现，之后必然继续漂移。

治本方案：

1. **单一构造器**：正文生成改走 `build_writing_context()`，与 `build_context()`
   同库同 `collect_items()`，只是排序规则从"问题相关度"换成"写作优先级"（见 4.1 的窗口组成）。
   `build_draft_user_prompt()` 退化为渲染器，不再自己挑选资料。蓝图读取统一走
   `documents._blueprint()`（最新 active 版本）。
2. **注入清单双通道可观测**：
   - 终端：`NOVEL_CONTEXT_DEBUG=1` 启动后端，每次生成打印清单
     （序号 / 层级 / 标签 / 字符数 / 是否被预算裁掉及原因），主人边跑边看。
   - 落库：`generation_run.input_summary` 存结构化 JSON 清单，
     `GET /api/novels/{id}/chapters/{chapter_id}/generation-runs` 可回读，
     按 7.4 的独立详情页范式展示。
3. **迭代回路**：清单每一项都带 `kind`（blueprint / toc / arc / brief / setting /
   character / foreshadow / summary / chapter）。主人指哪个 kind 该删或该加，
   只改对应 collector，不改调用方，也不改提示词模板。

## 7. 设定库

设定库不是静态百科，而是“随章节演进的事实系统”。

### 7.1 基础实体

- 世界观：规则、力量体系、历史、禁忌、重大事件。
- 地点：位置、归属、功能、当前状态。
- 势力：目标、关系、层级、当前状态。
- 物品：用途、归属、限制、状态。
- 伏笔：内容、埋设章、预计回收章、状态。

### 7.2 人物卡片

人物采用卡片管理，并支持动态分级：

| 级别 | 典型跨度 | 用途 |
|---|---|---|
| 主角团 | 全书或长期 | 深度档案、目标、关系、行为约束。 |
| 重要配角 | 多卷或长剧情弧 | 关键动机、关系变化、重要事件。 |
| 小 Boss / 剧情弧反派 | 10-30 章 | 本章弧内的目标、威胁、结局。 |
| 龙套 / 功能角色 | 1-几章 | 基本身份、功能、退场方式。 |

人物分级不必与章节严格绑定。例如长期人物可以很晚才出现，龙套也可以跨几章。系统应记录“预计出现窗口”和“实际出现章节”，当实际使用明显偏离预期时提示作者确认。

### 7.3 世界观地图

世界观地图支持后续调用生图模型生成图片。v1 可以先存位置卡片和关系，图片生成放在设定库稳定之后。

生成地图时必须遵循：

1. 只使用已确认的设定事实。
2. 生成结果标记为“参考图”，不是事实来源。
3. 作者确认后才能关联到对应地点卡片。

### 7.4 绘画详情：独立页面，不做底栏

主人 2026-09-02 批注：详情做成底栏形态不推荐，最好是一个能单独打开的页面，
显示消耗 token 以及一些其他信息。

- 独立整屏路由 `/novels/:id/artworks/:artworkId`，打开时**替换整个工作区**，
  不再嵌在右栏或底部条带里；顶栏只保留返回面包屑。
- 页面必须显示：结果大图、提示词、生成历史与版本对比，以及**本次调用的成本事实**——
  模型、输入/输出 token、耗时、成本估算，数据源 `generation_run`。
- 同一范式适用于正文生成详情：任何一次 AI 调用都要能在自己的页面上看到
  token 消耗与注入上下文清单（6.1）。

## 8. 数据范围

v1 至少需要：

- `novel`：书名、简介、目标篇幅、文风约束。
- `planning_blueprint`：A 层全书蓝图。
- `toc_entry`：B 层目录项、顺序、章节名、剧情功能。
- `arc_plan`：C 层剧情弧、起止章、目标、冲突、状态。
- `chapter_brief`：D 层单章简报。
- `chapter`：章号、标题、状态、正文、终审结论。
- `setting`：世界观、势力、地点、规则、禁用事实。
- `character`：姓名、级别、身份、目标、关系、当前状态、预计出现窗口。
- `character_appearance`：人物实际出现章节。
- `foreshadow`：伏笔、埋设章节、预计回收章节、状态。
- `chapter_summary`：章摘要、事件、状态变化、关联伏笔。
- `plot_feedback`：作者反馈、影响层级、建议修改、确认状态、应用时间。
- `generation_run`：任务类型、模型、prompt 版本、输入摘要、输出、token、成本。
- `review`：AI 自检结果、人工评论、决定、时间。

## 9. MVP 切法

### Phase 1：UI 骨架与核心写作环

1. 书架首页（封面卡片墙 + 新建向导）。
2. 工作台三栏布局与双根树（规划 / 设定库）。
3. A/B/C/D 四层规划的基础编辑。
4. CodeMirror 6 编辑器（原 TipTap 选型作废，见 X-04）：双栏对照生成、选区 Diff 修改、内联审稿、Ctrl+F、自动保存、版本历史。
5. 字数、禁用词、黑名单、必要字段机械校验。
6. AI 七维自检，要求引用原文证据；审稿意见内联锚定正文。
7. 对话 Agent：斜杠命令、@引用、流式消息，与写文 AI 分离配置。
8. 人物卡片 v1（分类标签、搜索、详情弹层）。
9. Token 用量明细记录与消息级展开。
10. 通过终审后写入章摘要、角色状态、伏笔状态。
11. 章节与简报的**显式新建入口**：树底固定按钮、树节点右键菜单、全局快捷键三通道，
    语义为「在末尾追加一章」，一次同时建出 Chapter 记录与 `chapters/NNNN/brief.md` 且章号一致（路径迁移随 S3 做，D-03）。
    新建章节是写作环的前置动作，不能依赖对话 Agent 顺带创建。
12. B 层目录列表视图与本地搜索（4.3，需先出帧）。
13. 正文生成上下文统一与注入清单可观测（6.1）。
14. 规划树按章归组的展示结构（4.2）。

### Phase 2：设定库、反馈与用量

1. /feedback 影响分析与确认后写回 A/B/C/D。
2. 世界观、地点、势力、物品、伏笔完整管理。
3. 角色状态时间线与事实冲突提示。
4. 独立世界观地图页与 AI 生图（生成历史、多版本对比）。
5. Token 用量汇总页（按功能 / 日期聚合，下钻单次调用）。
6. 树节点右键快捷操作补全：重命名、删除、在中间插入章节（章号顺延牵动文件名、
   B 层锚点、C 层弧范围与正文外键，代价见文末附录 1.6，故排在 Phase 2）。

### Phase 3：长程与视觉辅助

1. 滚动摘要与全书递进摘要。
2. 章节间一致性检查。
3. 规划层之间的一致性检查。
4. AI 生成书封与人物头像。
5. 更完整的质量面板。

## 10. 成功标准

连续完成 10 章后评估：

1. 至少 8 章不需要大幅重写。
2. 人工审稿和直接编辑的平均时间低于作者手工整理上下文并重写的时间。
3. 设定冲突能在下一章或下两章内被发现。
4. 伏笔状态不会因为几章之后遗忘而丢失。
5. ABCD 规划层之间不会因为作者反馈而频繁互相矛盾。
6. 作者愿意继续用这套流程推进第 11 章。

## 11. 已确认决策

1. AI 自检做完整七维。
2. 采用 A/B/C/D 四层规划。
3. 人工终审允许直接编辑正文，并做专门的写作页面。
4. v1 提供作品列表。
5. 设定库采用人物卡片和动态分级。
6. 世界观地图生图延后到 Phase 3。
7. 界面交互范式参考 Codex 与 VSCode：书架首页 → 工作台（双根树 / 对话流 / 编辑器）。
8. 常用能力做成斜杠命令，自由对话兜底。实测已实现 7 个：/generate /review /check /summary /save /plan /feedback；`/search`（联网搜索）**未实现**，归 S2。
9. 对话 Agent 与写文 AI 分离，独立模型配置，避免上下文污染。
10. Agent 上下文采用自动检索 + @引用。
11. 正文生成采用双栏对照、手动合并；选区修改采用 Diff 逐处接受/拒绝。
12. 人物卡片先选分类再陈列，支持搜索；点卡片弹出详情层。
13. 反馈通过 /feedback 命令或自由对话进入，确认后写回规划层。
14. 世界观地图采用设定库下独立地图页，生图放第二阶段。
15. Token 用量参照 OpenCode：消息级明细 + 汇总页。
16. 视觉采用深浅双主题，正文衬线、工作区无衬线。
17. C 层存储是弧对象序列，滑动窗口在生成时派生、不落盘（4.1）。
18. 文件树按章归组：`chapters/0042/{draft.md,brief.md}`；DB 仍是唯一真源，
    磁盘镜像单向导出排 P2（4.2）。
19. B 层目录默认可搜索列表视图，MD 文本降为源码视图，存储不变（4.3）。
20. AI 调用详情（绘画、正文生成）一律独立整屏页面，禁止底栏/嵌入式条带（7.4）。
21. 上下文注入只有一个构造器，注入清单必须双通道可观测（终端 + 落库），
    主人按清单增删机制（6.1）。

---

## 附：功能拆解与验收勾选（原 `docs/REQUIREMENTS.md`，2026-09-07 并入本文件）

这份文件原来单独占一个文件，和 PRD 说的是同一件事 - 两处各写一遍迟早打架。
并进来以后需求与勾选只有一份。下面每一节的标题比原文多一级 `#`，为的是不顶掉本文件自己的层级。
## 需求清单

对应 PRD v0.4 的功能拆解与勾选。设计理由见 DECISIONS.md。完成一项勾一项；需求变更先改 PRD，再同步到这里。

图例：`v1` = 本方案下第一个可用版本（**在现有后端上收口，不推倒重来**）；`P2` = 第二阶段；`S3`/`S2` = 推进主干步骤，见 WORKSTREAM-PLAN。

### 1. 页面与导航

- [ ] v1 书架首页：封面卡片墙、作品统计、新建作品向导
- [ ] v1 工作台三栏布局：左双根树 / 中对话流 / 右编辑器
- [ ] v1 双根树：规划（A/B/C/D 层级）与设定库两个根节点
- [ ] v1 树节点状态徽章：草稿 / 已审 / 定稿（中文标签）
- [ ] v1 书架删除作品（输书名确认）—— **前后端都未实现**：`BookshelfPage` 零命中、
      后端无 `DELETE /api/novels/{id}`；需先定 15 表级联策略（DECISIONS 5.2），排 S3 之后
- [ ] P2 树节点右键快捷操作补全（重命名、删除、拖拽排序）

#### 1.6 新建章节与简报（2026-09-02 主人重述需求后展开）

主人原话口径：**新建章节要像新建文件夹一样有显式入口**——要么右键打开新建，要么有新建
按钮，也可参考 VSCode 文件栏那种「一堆快捷键 + 右键」的形态。不是「点一下最新章节就自动
建下一章」（该理解已被否决）。

三通道入口（任一都能完成同一件事，共享同一个 store action）：

- [x] v1 树底固定图标排 `新建章节`（`58b8da4` 三通道入口已落地）
- [x] v1 右键「单章简报」分组标题 → 菜单含 `新建下一章简报`（`58b8da4`）
- [ ] v1 右键某个章节/简报行 → 菜单含 `在其后新建章节`（置灰并标注 P2，见下方插入代价）
- [x] v1 全局快捷键 `Ctrl+Alt+N` = 新建下一章简报（`58b8da4`）
- [ ] v1 空态：D 层无章节时显示「还没有章节」+ 内联 `新建第一章`

语义与边界：

- [x] 新建 = **同时**创建 `Chapter(chapter_number = 现有最大 + 1)` 与 `chapters/{N:04d}/brief.md`；
  已改走文件层 `PUT /files/{path}`，首写简报时同事务建正文行（D-01）
  二者章号必须一致；只建其一视为 bug
- [ ] 章号策略 v1 **只允许末尾追加**。中间插入要连带改简报文件名、B 层目录锚点章号、
  C 层弧的章节范围、已生成正文的 `chapter_number` 外键——漏一处即数据不一致，故列 P2
- [ ] 新建成功后：左树出现该章 → 自动打开该简报文件 → 光标落在 `## 目标` 行
- [ ] 不弹对话框收标题：`Chapter.title` 先留空，由简报内容反推标题回填列 P2
- [ ] 防重：请求未返回前入口全部 disabled，连点 5 次只建 1 章
- [ ] 后端 409（`ensure_chapter_number_unique`）时 toast 显示 detail 原文，不静默
- [ ] 后端不可用时点击给失败 toast，与底部「后端 已连接」状态条口径一致
- [ ] 刷新后新建的章节仍在（服务端单一真源，不接受前端伪造行）

验收：上述每条都要有对应测试或可复现步骤；`npm run build` 与前后端测试全绿。

#### 1.7 规划树按章归组（2026-09-02 主人批注 3）

主人方案：四层都在一个文件夹里管理，正文与 D 层规划放在同一章目录下。方向采纳，
但 PRD 4.2 已记录前提：现有四个 `.md` **是 DB 投影，不是磁盘文件**，所以归组落在树的展示层。

- [x] v1 树结构改为 `规划/chapters/{N}/{draft.md,brief.md}`：一章一目录，正文与简报同组
- [x] v1 旧路径 `chapters/{N}` 与 `briefs/{NNNN}.md` 继续可解析，不断链、不迁移数据
- [x] v1 `documents.py` 的路径正则同时接受新旧两种写法（读旧写新）
- [x] v1 数据库与 API 契约不变：`chapter` 与 `chapter_brief` 仍是两张表，靠 `chapter_number` 配对
- [ ] P2 磁盘单向导出镜像 `exports/<书名>/…`，可 git、可手改；读仍以 DB 为准
- [ ] P2 双向同步（磁盘改 → 回写 DB）：需要先设计冲突解决，v1 明确不做

C 层滑动窗口的结论见 PRD 4.1（弧是存储、窗口是派生视图），本节只处理"文件怎么摆"。

#### 1.8 B 层目录列表视图与搜索（2026-09-02 主人批注 1，帧 21 已批准并落地）

主人原话：目录只是 MD 形式，不方便搜索管理，也不好看；要做成列表，之后要做搜索功能。

- [x] v1 `toc.md` 默认**列表视图**：列 = 章号 / 章名 / 剧情功能 / 该章正文状态
- [x] v1 行内编辑章名与剧情功能，保存走现有 `PUT /files/toc.md` 的解析回写通道
- [ ] v1 章号是主键：列表里不可改号、不可删行下线章节（与 MD 视图同一套锁规则，422 一致）
- [x] v1 搜索框：按章名 + 剧情功能 + 备注本地过滤，命中字段高亮
      （自动滚动定位仍待补）
- [x] v1 「源码」开关切回 `toc.md` 文本视图，两视图共用同一份数据，不产生第二真源
- [x] v1 无结果空态给 `清除搜索`；目录为空时的「从蓝图生成本卷目录」引导仍待 AI 提案通道
- [ ] P2 跨作品全文检索
- [ ] P2 拖拽调序（连带章号顺延，代价见 1.6，仍排 P2）

### 2. 对话 Agent

- [ ] v1 混合对话流：流水线消息卡片 + 自由追问
- [x] v1 斜杠命令（实测 7 个，`ChatPane.tsx:76`）：/generate /review /check /summary /save /plan /feedback
- [ ] v1 `/search`（联网搜索）—— **未实现**，后端零命中，归 S2
- [ ] v1 斜杠命令面板：输入 / 弹出候选、支持过滤
- [ ] v1 @引用：像 Codex 一样引用规划/设定/章节节点
- [ ] v1 流式消息更新
- [ ] v1 对话 Agent 与写文 AI 分离，独立模型配置（新增 `NOVEL_LLM_CHAT_MODEL`）
- [ ] P2 /feedback 影响分析：定位受影响层级、生成修改建议
- [ ] P2 反馈确认后写回 A/B/C/D 对应层并记录规划版本

#### 2.5 agentic 内核（2026-09-03 新增，**当前完成度 = 0**）

本节存在的原因：上一版文档把 §2 全写成「对话 Agent 已落地」，导致它的完成度被普遍高估。
实测边界：`tool_call` / `function_call` / `web_search` 在整个 backend **零命中**；一次请求＝一次
LLM 调用＝一路文本回来即结束。已有的是外壳（多轮窗口 8、落库、SSE、@引用、token、提案抽取）。

- [ ] v1 工具注册表：声明式工具清单，参数 schema 校验，返回值作为下一圈观察
- [ ] v1 多步循环：LLM 输出→若含工具调用则执行→结果回填→再问，直到给出终答；
      每圈步数与 token 上限，超限明确报错，不静默截断
- [ ] v1 联网搜索工具（补上一直列在文档里但从未实现的 `/search`）
- [ ] v1 本系统内部命令：读规划 / 读章节正文 / 查伏笔与人物状态 / **按规范修改四层规划**
- [ ] 红线：Agent 写规划只能调 S3 收口后的那一条通路，且 `actor=ai`（受 D-10 白名单约束，
      不许增删条目、不许改小节标题）
- [ ] 复用而非重写：`prepare_turn()` 已是「取历史 + 选上下文 + 组 messages」，即循环每圈的输入侧
- [ ] 验收：一句自然语言「把 45-48 章收进第二个弧并补钩子」→ 它自己走 读 → 提案 → 人点应用，
      全程不产生第二条写通路，且注入清单能看出它读了什么

禁止事项：在 agentic 内核存在之前，任何文档、提交说明、对话回复都不许把 §2 的能力描述成「Agent」
或「智能体已实现」。要说就说「对话外壳 + 上下文通路」。

### 3. 编辑器

- [ ] v1 正文编辑器：规划文档侧已用 CodeMirror 6 落地（`cmDoc.ts`）；正文侧仍是 textarea，
      待按 D-08 换成 CodeMirror 6 并接自动保存（原 TipTap 方案作废，见 X-04）
- [ ] v1 双栏对照生成：/generate 新旧正文并排、手动合并
      （2026-09-03：正文 SSE 已接通，编辑器/文件缓冲可实时追加；双栏对照页仍待做）
- [ ] v1 选区修改：框选 → 指令 AI 修改 → Diff 对照逐处接受/拒绝
- [ ] v1 内联审稿：审稿意见锚定正文片段，高亮 + 批注气泡
- [ ] v1 审稿侧栏：展开完整七维审稿报告（一致性/角色/节奏/连贯/伏笔/钩子/文风）
- [ ] v1 Ctrl+F 快速查找
- [ ] v1 自动保存（防抖 + 保存状态指示）
- [ ] v1 版本历史侧栏（人工编辑保存为新版本）

### 4. 设定库

- [ ] v1 人物分类标签页：全部 / 主角团 / 重要配角 / 小 Boss / 龙套
- [ ] v1 人物搜索
- [ ] v1 人物卡片墙 + 弹出详情层（头像、活跃章节、弧线、关系）
- [ ] v1 人物活跃章节范围与规划联动（预计窗口 + 实际出现）
- [ ] P2 世界观 / 伏笔 / 地点 / 阵营完整管理页
- [ ] P2 事实冲突提示

#### 4.5 绘画详情独立页（2026-09-02 主人批注 4，帧 22 已批准并落地页面壳）

主人原话：把绘画详情页做成底栏形式不推荐，最好是一个能单独打开的页面，显示消耗的 token
以及一些其他信息。

- [x] v1 独立整屏路由 `/novels/:id/artworks/:artworkId`，打开时替换整个工作区，
  不再是右栏内的面板，也不是底部条带；原嵌入面板已改为整屏路由壳
- [ ] v1 页内显示成本事实：模型、输入 token、输出 token、耗时、成本估算（源 `generation_run`）
- [ ] v1 保留：大图预览、提示词面板、生成历史时间线、版本对比、下载 / 设为参考
- [x] v1 顶栏返回按钮回到地图列表；`Esc` 等价返回仍待补
- [ ] v1 同一范式适用于**正文生成详情页**：一次 AI 调用 = 一个可单独打开的页面
- [ ] P2 生图后端接入（现为页面壳）

### 5. Token 用量

- [ ] v1 每次 LLM 调用落一条用量明细（功能、模型、输入/输出 token、耗时）
- [ ] v1 对话流消息级展开用量明细（参照 OpenCode 对话详情）
- [x] v1 正文生成 run 的**注入上下文清单**可在整屏详情页展开（帧 23 / 10.3）；
      新记录同时保存每块 excerpt，点选后可见实际注入原文。
      对话与 fact_extract run 的统一详情路由仍待补。
- [ ] P2 用量汇总页：按功能 / 日期聚合，可下钻单次调用

### 6. 世界观地图

- [ ] P2 设定库下独立地图页
- [ ] P2 AI 生图 + 生成历史 + 多版本对比保留

### 7. 视觉与主题

- [ ] v1 深浅双主题，默认跟随系统
- [ ] v1 正文衬线字体、工作区无衬线
- [ ] v1 状态枚举全部中文化展示

### 8. 质量门槛

- [ ] v1 后端 pytest 全绿、前端 Vitest 全绿、`npm run build` 通过
- [ ] v1 每个页面 Playwright 截图给作者过目
- [ ] v1 每个可验证单元独立 commit + push

### 9. 技术选型（2026-08-30 调研结论）

按 search-first 决策矩阵得出：优先采用成熟开源件，找不到合适积木的场景才自研。

#### 采用（ADOPT）

| 场景 | 选型 | 依据 |
|---|---|---|
| 斜杠命令面板 + @引用选择器 | cmdk（~12.9k stars, MIT） | 无样式命令菜单，键盘导航完善，Codex 命令面板同类实现 |
| 双根树 | react-arborist（~3.7k stars, MIT） | 完整 React 树组件，支持折叠、虚拟滚动、选中态 |
| 正文编辑器 | **CodeMirror 6**（已采用，`codemirror@6.0.2`） | 原选型 TipTap 作废（X-04）；规划文档扩展在 `cmDoc.ts` |
| 散文 Diff | diff（jsdiff）+ 自渲染 | 小说级段落对比不需要 Monaco 这类重量级代码 Diff 编辑器 |

#### 待定（C2 架构评审决策）

- 组件基座：引入 shadcn/ui（Tailwind + Radix）替换手写 CSS，还是保留现有 `styles.css` 仅用上述积木库。涉及前端基建改造，归 C2 架构评审统一裁决。

#### 自研（BUILD，无现成同类件）

- 双栏对照生成 + 逐段合并视图
- 内联审稿锚点 + 批注气泡（小说场景）
- A/B/C/D 双根树的数据模型层

#### 同类项目参考

| 项目 | 定位 | 可借鉴 | 不采用 |
|---|---|---|---|
| Nigh/show-me-the-story（521 stars, Go+Svelte） | 自托管 AI 小说生成器，纲→逐章→审稿→伏笔→事实核验 | 流水线阶段可视化、全书润色批次 | Svelte 技术栈不可直接复用 |
| sigpanic/goink（193 stars, Wails 桌面） | 对话式写作 + 自动状态追踪 + 本地语义搜索 | 对话驱动 + 状态追踪的组合范式 | 桌面端架构不同 |
| SillyTavern（~32.8k stars） | LLM 前端，角色卡片生态成熟 | 人物卡片字段设计、卡片导入导出 | 面向角色扮演聊天，不是长篇工作台 |

调研结论：GitHub 上没有可整体照抄的"长篇小说工作台"Web UI，Codex/VSCode 混合范式即差异化方向，只取各家的积木与范式，不搬整套代码。

#### 由调研新增的候选需求

- [ ] P2 人物卡片导入/导出（参考 SillyTavern 卡片范式）
- [ ] P3 全书润色批次任务（参考 show-me-the-story）
- [ ] P3 本地语义搜索章节与设定（参考 goink，备选）

### 10. 上下文注入与可观测（2026-09-02 主人批注 5，**最高优先，后端不待帧**）

主人原话口径：分层管理与"各种设定如何合理注入上下文"是最核心最有创意的业务逻辑，
要兼顾成本、不忘关键内容、保证每章逻辑连续；担心实现乱做。
验收方式由主人指定：调成写作模式让 AI 写某一章时，**能在终端或某处看到本次续写实际注入的
上下文内容**，据此要求删块或加块，再改机制。

#### 10.1 先修割裂（治本，PRD 6.1 已记录取证）

- [x] 取证：正文生成与对话 Agent 是两条通路，写作侧没有伏笔 / 人物状态 / 上一章结尾
- [x] v1 正文生成改走与对话 Agent 同一个 collector 池（`collect_items()`），
  `build_draft_user_prompt()` 退化为渲染器，不再自行挑选资料
- [x] v1 废除第二真源：`generate_from_brief` 取蓝图必须用 `documents._blueprint()`
  （最新 active 版本）；现状 `order_by(version)` 升序取**最旧**，属缺陷
- [x] v1 对话侧 `build_context()` 与写作侧 `build_writing_context()` 共享预算常量与裁剪
  逻辑，只有排序规则不同：两侧排序后都交给 `apply_context_budget()`；
  `TIER_CORE` 恒注入，非核心块放不下即裁并写明原因。

#### 10.2 写作优先级窗口（PRD 4.1 的落地）

- [x] v1 必注入（永不被预算裁掉）：作品信息与文风约束、A 蓝图、D 本章简报
- [x] v1 次优先：当前弧、上一章结尾、出场人物的当前状态、**所有未回收伏笔**
- [x] v1 第三档：B 目录邻域（N±3 章）、近 5 章章摘要、被点名设定
- [x] v1 填充档：其余按相关度，预算不足时从这一档开始裁
- [x] v1 不变量测试：预算再紧也不得裁掉未回收伏笔与出场人物状态
- [ ] P2 弧收束摘要 / 卷摘要（分层摘要塔，窗口外事实的压缩来源）

#### 10.3 双通道可观测

- [x] v1 `NOVEL_CONTEXT_DEBUG=1` 时后端终端打印注入清单，正文生成与对话两条链路都打：
  序号 / kind / 标签 / 字符数 / 是否被裁 + 原因，一次生成一段
- [x] v1 `generation_run.input_summary` 存结构化 JSON 清单（`budget` / `used` /
  `blocks[]`，每块含 `kind`、`label`、`chars`、`dropped`、`reason`）
- [x] v1 清单可经 `GET /api/novels/{id}/chapters/{chapter_id}/generation-runs` 回读
- [x] v1 后端测试覆盖：清单 JSON 可解析、块顺序符合 10.2、dropped 记录正确
- [x] v1 空内容不得显示为已注入：必注入档若内容为空，须落入未注入区并写明原因
      （2026-09-03 跑真实清单时发现的缺陷，见 DECISIONS T-14；此前空简报会以「必注入 · 0 字」混在清单里）
- [x] v1 旧格式兼容：`input_summary` 已有的 `ChapterBrief:12` 纯文本读侧不得报错
- [ ] P2 详情页把清单渲染成可折叠列表（依赖 4.5 的独立页范式）

#### 10.4 主人的迭代回路

- [x] v1 主人指认某个 `kind` 多余或缺失时，只改对应 collector，
  不改调用方、不改提示词模板、不改前端
- [x] v1 每次改动补一条断言清单内容的测试，防回退
