# 推进计划书

**本文件只记进度与勾选。** 已完成的历史纪要一律进 `DECISIONS.md`，不再往本文件末尾追加流水
（旧版涨到 619 行 / 51KB、C6 与 C7 各撞号两次，就是这个习惯造成的）。

工作规则：

1. 开工先读本文件《状态看板》+《主干》，再读 `ARCHITECTURE.md` 确认现状与缺口。
2. 只做当前 S 步骤范围内的任务，别的发现只记录不顺手改。
3. 每完成一项打勾；改需求先改 PRD，再同步 REQUIREMENTS / UI-DESIGN；理由进 DECISIONS。
4. 每步收尾必须：后端 pytest + 前端 vitest + build 全绿，commit + push（走代理）。

---

## 状态看板

当前：**UI 支线**——第十五批 13 件事已全部落地并推送，进入第十六批（2026-09-05）。

门禁实测（同日）：后端 `180 passed`｜前端 `125 passed / 18 files`｜`tsc -b --force` 干净｜
`build` 干净｜`.scratch/hit-area-audit.mjs` 0 small / 0 clipped / 0 unreachable。
不可回退闸门 `frontend/src/uiInvariants.test.ts` **20 块**。

- 主干与 UI 支线**可并行**，但 S3 必须早于 S2，理由见 `ARCHITECTURE.md` §5。
- 界面未结条目一律记在 `UI-BACKLOG.md`，本文件不再复述（各管一段）。

---

## 一、主干（S 系列）

### S0 上下文可观测演示

- [x] 单构造器 + 四档排序 + 清单双通道（后端 `c385f5b` / `b397081`）
- [x] 后端跑在可见终端、控制台 UTF-8
- [x] 只读跑出两份真实清单（novel 1 第 43 章 944/12000；novel 3 第 43 章 83/12000）
- [x] 清单当场抓到缺陷：空简报冒充「必注入 0 字」→ 已修（装配层过滤 + 测试，102 passed，见 T-14）
- [ ] 走 `/generate` 全链真调一次 API（会写正文与 generation_run，等主人点头）
- [ ] 主人据清单指认「哪个 kind 该删 / 该加」，形成下一批 collector 调整

验收：主人在终端亲眼看到一份清单，并能就着它说出至少一条增删意见。

### S1 最小写作环

- [x] 一个隔离脚本：空临时库 → 建一部小说 → 文件层写规划 → 一次注入 → 出正文
- [x] 脚本中途打印注入清单；2026-09-03 实测 9 块 / 354 字，`PASS`
- [x] 对话侧与写作侧的预算常量、裁剪循环合并成一份（REQUIREMENTS 10.1 末条）：
      两种排序结果统一交给 `apply_context_budget()`
- [x] 脚本入库为 `backend/scripts/writing_ring_smoke.py`，可重复执行；
      默认 `offline-template` 不耗 token，`--live` 才调真实 draft 模型

验收：一条命令跑通，输出含正文与清单，退出码 0。

### S3 写通路收口 + 路径迁移（★ 必须先于 S2）

主人 2026-09-03 裁定：B 路为唯一写入口（D-01），路径迁移同批做（D-03）。

- [x] 先写测试：`/planning/*` 的 POST/PUT 不可再用（返回 410）；`resolve_path` 新旧路径双向可解析
- [x] 新建章节改走文件层：一次写入同事务建 `Chapter` 与 `chapters/{N}/brief.md`
- [x] 文件层补齐「新建文件」能力：`chapters/{N}/brief.md` 首写自动建简报与正文行
- [x] `resolve_path` 支持 `chapters/0042/brief.md`，**读旧写新**，旧 `briefs/0042.md` 仍可读
- [x] 正文投影成 `chapters/0042/draft.md`（`EditorPane` / 对话 `/save` 与文件层同一条写通路；
      旧 `POST / PUT /chapters...` 已返回 410）
- [x] `routers/planning.py` 退役写端点，只读 GET 按需保留
- [x] 前端 `store/files.ts` 路径常量与左树 `briefs/` 分组换成按章归组
- [ ] 因退役而变无用的导入/助手删掉（不顺手删既有死样式 → 归 U7）

验收：全仓只剩一条写四层规划的路径；`/planning/*` 只剩 GET；新旧路径往返测试通过；
后端测试数不降（≥101），前端 52 全绿，build 干净。

### S2 agentic 内核

- [x] 工具注册表 + 多步循环：`services/agent.py` 的 `ToolRegistry` 与 `stream_agent_turn`。
      **协议是自己定义的文本块而不是 OpenAI `tools`**——本机网关实测 `finish_reason: stop` 且回包
      **没有 `tool_calls` 字段**，MiniMax-M2.5 把调用写成私有 XML 混在 `content` 里；照原生通道写
      循环永不触发、还会把 XML 泄漏进正文。`tool_calls` 若存在仍一并识别。
      流式侧带 hold-back：未闭合围栏之后的文字一律先扣住，控制块永不上屏（`done` 事件不得冲掉工具轨迹，已补测试）。
- [x] 联网搜索工具：`web_search` 走中文维基百科检索（免密钥、实测可用）；前端补 `/search <词>` 命令。
      DuckDuckGo lite/html 本机实测 **403**，所以通用网页结果需要另配搜索 API，没有凭据时如实报失败而不是编结果。
- [x] 本系统内部命令：`list_files` / `read_file` 读的就是文件层那一份文档（同一个 `documents.read_file`）。
      **注册表里没有写工具**（有测试钉住名字集合）：改规划仍只有「提案 → 主人点应用 → `actor=ai`」这一条，
      与 D-01 / D-15 不冲突。
- [x] `prepare_turn()` 复用为循环每圈的输入侧：循环只在它给的 messages 上追加，不重建上下文装配。
- [x] 每圈步数与 token 上限（默认 4 步 / 30000 token，`AgentConfig` 可调），**超限抛 `AgentBudgetError`**：
      SSE 出 `error` 且**不落库半截回答**（`test_agent_chat.py` 钉住「error 有、done 无、assistant 行为空」）。

验收：一句自然语言「把 45-48 章收进第二个弧并补钩子」→ Agent 自己走 读 → 提案 → 人点应用，
全程不产生第二条写通路，且注入清单能看出它读了什么。
验收改在**为本轮新建的书**上跑（`观星残卷（流程测试）`：A/B/C 三层 + 三章简报 + 人物 + 伏笔册 + 世界观册），
因为主人原话那句需要「已有第二个弧」才成立，novel 1 构造不出。同一形态在测试书上跑通了：

- 真模型一轮「弧 1 收束太弱，先读 arcs.md 和伏笔册再提改法」→ **2 步 read_file → 1 条 arcs.md 提案 → 人点应用（actor=ai，`changed=["3.resolution"]`）**；
  回答真的把弧的收束与伏笔册的「缺名 / 碑屑」串了起来，不是复述。全程无控制块上屏。
- `/generate` 同章产出 1649 字（run 15），调用详情页如实显示「机械校验未通过 · 缺少必要事实：沈砚舟左眼渐盲」。
- 注入清单 18 块里**伏笔两条、世界观设定两条各占一行**（第 10/11/17/18 行），D-15 两册确实进了上下文池，U4 的「伏笔档永远空」闭合。
- 跑流程过程中抓到并修掉三个真缺陷：模型换用第三种工具方言导致控制块泄漏；重复读同一份文件烧光步数预算；
  改主键的提案卡片仍显示 valid 并给「应用」按钮（点下去才 422）。见 DECISIONS T 系列与三次提交。

**仍未验到**：主人原话那句字面场景（45-48 章）没跑过；跨弧「移动章节」需要 D-13 的章号策略先定。
**新发现的产品口径问题（未擅自改）**：`required_facts` 按**字面子串**匹配，简报写「沈砚舟左眼渐盲」而正文写「他左眼已看不太清」即判缺失。
要么简报事实改成可匹配的关键词，要么校验换成语义级判定——请主人定方向。

### S4 合流

- [ ] 对话式管理接上写作环（Agent 能触发 S1 那条链）
- [ ] Ctrl+K 全局命令面板（旧 C4a 遗留）

---

## 二、UI 支线（U 系列，不阻塞主干）

### U1 编辑器高级能力（旧 C4）

- [ ] 双栏对照生成（`/generate` 流式，逐段合并）— Figma 帧 05 已批，代码未做
- [x] `/generate` SSE 流式接通：正文编辑器与 `draft.md` 缓冲在生成中实时追加；
      双栏对照与逐段合并仍待做
- [ ] 选区修改（浮动工具条 → Diff → 逐处接受/拒绝）
- [ ] 内联审稿（高亮锚点 + 批注气泡 + 七维报告抽屉）
- [ ] Ctrl+F 查找条 + 全量快捷键接线
- [ ] 版本历史侧栏；自动保存状态指示

### U2 帧 21：B 层目录列表视图与搜索（已批准，主体落地）

- [x] 帧 21 已出（节点 `460:6`），自查修掉两处缺陷：保存按钮溢出 596 宽面板、控件内文字底部切掉
- [x] 批准后实现：列表视图 + 行内改名即存 + 本地搜索 + 源码开关（两视图共用一份数据，不产生第二真源）
      命中字段已高亮；自动滚动定位待补

### U3 帧 22：绘画详情独立页（已批准，页面壳落地）

- [x] 帧 22 已出（节点 `464:6`），假模型名已改为「文生图模型（待接）」
- [x] 批准后先实现独立路由、整屏页面壳、成本/提示词/历史展示（后端生图能力 = 0，数据仍为页面壳假数据；
      `generation_run` 真源接入和 `Esc` 返回待补）

### U4 设定库与用量（旧 C6）

- [ ] 活跃章节与规划联动展示
- [x] 正文 run 整屏调用详情（帧 23）：`/generate` 命令卡和调用记录双入口；
      成本 KPI、结构化注入清单、原文摘录、输出、机械校验和审稿分 tab 展示。
      旧记录无 `excerpt` 时明确提示；耗时埋点和对全部 AI run 的统一路由待补。
- [ ] Token 用量明细核对；汇总页（按功能/日期聚合，下钻单次调用）
- [x] 伏笔写入端点：`settings/foreshadow.md` 经唯一文件层写入口落库，注入清单里的伏笔档不再是永久空
      （`chat/context?kind=foreshadow` 实测返回新建的两条）。**仍无删除端点**：文档里删一行不会下线伏笔，
      与 `弧 N` / 目录同规则，需要删除时得另定策略。

### U5 整体验收（旧 C7）

- [ ] 全页面真实浏览器截图给主人过目
- [ ] REQUIREMENTS.md 的 v1 条目全部勾完
- [ ] CI 绿 + 推送

### U6 反馈与写回（Phase 2）

- [ ] `/feedback` 影响分析 → 确认后写回 A/B/C/D（写回必须走 B 路入口）
- [ ] 角色状态时间线与事实冲突提示；AI 自检主动插「建议反馈」卡片
- [ ] 树节点右键快捷操作补全：重命名、删除、中间插入章节（顺延代价见 D-13）

### U7 清理与运维

- [ ] ⚠ **纪律闸门仍未生效（等主人）**：助手在同一轮内 5 次把一句并不存在的工具拒绝写成正文
      （探针实测该串在真实回包中命中 0 次、在自述文本中 19→21 次）。文字规则拦不住这个通道，
      机械拦截脚本已就位但需主人在终端版 codex 跑 `/hooks`、逐条看清后 trust
      `python ~/.codex/scripts/dedupe_gate.py` 那一条（**别按 t 全信**，ECC 自带 22 条第三方 hook）。
      模型无法自行 trust。
- [ ] **`DELETE /api/novels/{id}/characters/{cid}` 不存在**：前端 `CharacterLibrary.remove()`
      一直在调它，必然 405 —— 人物「删除」按钮是坏的。与「删除作品」同属删除语义那条决策。
- [ ] **伏笔墙 / 地图面板未接线**：面板内**零请求**，所以 `foreshadow.md` 里已有两条、注入清单也
      能看到，它仍显示空态。这是面板未接线，不是数据没进来——别再当「伏笔功能没做」重复排查。
- [ ] **novel 3「MD探针」测试作品**：等上面那条端点补齐后删除。
- [x] 死样式类扫描：`34f36ef` 已清除（Q-01）；2026-09-05 再扫，`styles.css` 370 个类名里
      25 个源码零命中。**逐个判生死，不整批删**：`cm-*` 由 CodeMirror 生成，`level-boss`／
      `level-protagonist` 来自 `` `character-card level-${character.level}` ``，
      `status-applied` 来自 `` `feedback-item status-${item.status}` ``（后端确有 `applied`）——
      这些都活着。真死的 13 个已删：`book-continue`／`check-result`／`cover-hint`／
      `editor-footer`／`editor-tab-label`／`file-mode-return`／`footer-toggle`／`form-grid`／
      `status-rejected`（后端从无此值）／`statusbar`／`topbar-center`／`tree-actions`／
      `tree-ellipsis`，共 26 条规则、`styles.css` −4.1KB。
      ⚠ 第一版删除脚本把**注释里的逗号**当成选择器分隔符，切断三处注释造成**未闭合注释**
      （会吞掉后面整段 CSS）——已回滚重写：先把注释换成占位符再解析。

---

## 三、未结事项（非 UI）

UI 的未结一律在 `UI-BACKLOG.md`，此处不复述。

- 删除语义一条决策未定，压着三件事：删除作品（`DELETE /api/novels/{id}` + 15 表级联）、
  删除人物（端点缺失）、以及历史脏行 `[TOOL_CALL]` 是否清（**2026-09-05 决定：不清**，
  理由见 DECISIONS **D-17 ③**，与 Q-02 同一条依据）。
- 绘画详情页右侧全是前端假数据，后端生图能力为 0（ARCHITECTURE §4 缺口 5）。
- 另有一个上一任误建的独立 Figma 文件 `rISrh8a5tXvSLnK2uGsXEl`（帧 24-26 旧稿），与主人看的
  v2 文件无关，确认无用后可整文件删除。

---

## 四、历史（已完成，只留一行结论）

细节、取证、废止口径全在 `DECISIONS.md`；某一批的完整过程可用
`git show <提交>:docs/WORKSTREAM-PLAN.md` 追溯。本表只留一行，是因为这个文件曾经
靠流水涨到 88KB，而涨到那个体积并没有让任何一条决定变得更清楚。

| 编号 | 主题 | 结论 |
|---|---|---|
| C1 | 调研与设计基础 | 组件选型与 UI 清单 v2 落文档 |
| C2 | 架构评审与框架适配 | 前端拆层（store/pages）+ 后端 service 层收敛 |
| C3 | Phase 1 骨架 | 路由 + 书架 + 工作台三栏 + 双根树 + 深浅双主题 |
| C4 | Phase 1 编辑器 | **未完**，剩余项移入 U1 |
| C4a-d | 工作台框架重写 / 九条批注 / Figma 整页 / 规划层文件化 | VSCode 式外壳落地；文档层后端契约完成 |
| C5 | Phase 1 对话 Agent | SSE 流式 + @引用 + token 记账 + 提案通道。**只是外壳，内核见 S2** |
| C5a-d | 浏览器批注第一~三轮 | 对话坞网格、视觉收尾、上下文统一与清单双通道（即 S0） |
| C6 | 文件面 YAML → Markdown | 纯投影层改动、零迁移；围栏兼容垫片（T-13） |
| C7 | 新建章节显式入口 | 三通道 + 右键菜单（`58b8da4`）；投影 bug 已修（T-07） |
| — | 网关 401 | **结论作废**：取证脚本截断了 base64 key 的 `=` 填充（R-01） |
| 09-03 一批 | 11 条中的 5 条 | 缩略栏 1:1 可拖、焦点环四边、拖拽手柄回 2px 细线、记录区可拖高 |
| 09-03 二批 | 6/8/9/10/11 | **全面废弃「三边无色、单边有色」**；真因是全局 `button{min-height:32px}` |
| 09-03 三批 | 20 条中的 16 条 | 树前缀 18→15px、图标化收起按钮、注入清单改 A/B/C/D 层、名词中文化 |
| 09-03 四批 | 第三轮 7 条，全真机量 | `min-height` 病根一次扫出 4 处；缩略栏 CSS/JS 两套几何统一；**StrictMode 让布尔守卫失效**那次记档 |
| 帧 24/25/26 | 转设计轨 | 帧 24 已落地；帧 25/26 待审批 |
| D-15 | 设定库进文件层 | 两单元完成：人物档案挂唯一写入口，旧 `POST/PUT /characters` 转 410 |
| 帧 27 | 左栏活动条 + 三页侧栏 | 三轮退回后批准落地（`428a6f0`） |
| 第五轮 21 条 | 去方框·去常驻·去重复 | 全部落地并真机复验，12 个提交；固化成 UI-DESIGN §0.7 |
| 第六轮 25 条 | 15 条落地 | 剩 10 条分三堆（小修／需出帧／等删除语义）。其中「17 调用记录面板按内容给高」**2026-09-05 核实为已完成**：面板默认折叠 + 可拖高 + 溢出滚动（实测 4 条记录时 `scrollHeight 281 > clientHeight 144`，高 168px 固定不再随内容空占），答复方式与主人当时的诉求一致（09-03 第四批把「每次进来都摊开」改成默认收起） |
| 第七轮（第九批） | 8 条 | Codex 式对话呈现（主人点了三次的那条）落地 |
| 第十批 | 7 条中的 6 条 | 深色「灰」按主人给的 #191A1B 重定，全应用色阶压深 |
| 第十一批 | 9 条 + 「思考过程还是没做」 | 工具轨迹前端折叠落地；**模型推理文本仍拿不到**（见第十六批 16.1） |
| 第十二批 | 2 条 3 件事 | 两条都是上一轮我自己造的：滚动条占布局、对话列滚动条占位 |
| 第十三批 | 四处「你改回去了」 | 装上机器闸门 `uiInvariants.test.ts`，从此改界面=同时改断言 |
| 第十四批 | 6 条 | 标签条三件事、章节点与未保存点同步、聚焦去主色（**只去了颜色，见第十五批 4.x**） |
| 第十五批 3.3 | 两份 buffer 互相覆盖 | 正文页改成 `draft.md` 文件条目的投影，`draftBody`↔`draftDocument` 按字节互逆 |
| 第十五批 1.3/1.4/3.2/3.4 | 一个文档一个钮 | 钮出现在它所在的每一条标签条上，两侧共用 `views[path]` 与 `toggleViewLabel()` |
| 第十五批 1.1/1.2 | 标签条真的溢出 | 真根因是 `.editor-pane` 缺有界列，不是收缩地板 |
| 第十五批 2.2 | 拖到极限关栏 | 三栏同一个 `applyPane` 判据；关闭幂等（第 12 次方向键曾把刚关的栏开回来） |
| 第十五批 2.1 | 工具栏动作不消失 | 换行 + 两处 `min-width:0`；顺带修掉 resize 不重渲染导致正文列被挤成 24px |
| 第十五批 3.1 | 人物文件渲染成卡片 | `CharacterDocCard` 解析结构行与小节；分级词只有一张表 |
| 第十五批 4.1/4.2 | 聚焦不画任何一圈 | 整类扫完，含 CodeMirror 自带 `.cm-focused` 描边；闸门升级为「先剥注释再查一类」 |
| 第十六批 16.5、16.6 | 提示语里的句号 | 全仓扫 `。` 命中 31 行，改 17 条界面提示、刻意保留 14 处（Agent 正文／发给模型的提示词／生图 prompt／注释）；闸门扫全部 `.tsx` 源码、白名单逐条点名不写模式匹配；固化成 UI-DESIGN §0.7 条九 |
| 第十六批 16.3 | 章节标签分不清 | 标签改显四位章号、章名搬到 `aria-label`/`title`；宽度三个数经实测无需改动（6 个标签时 `scrollWidth == clientWidth`）；两条既有测试改断言不删 |
| 第十六批 16.1 | 回复上方的思考过程入口 | `reasoning_out` 出参累加器（照 `usage_out` 写法，不改 yield 契约）→ agent 跨步骤透传 → `chat_message.reasoning`（迁移 `f3b9c0d21a55`）→ `ChatMessageOut` → 前端折叠；顺手把「思考过程」与「工具轨迹」两个名字分家（Brain / Wrench）；两个只有真机才暴露的自造 bug 记档：delta 块被 `\n\n` 拼碎、出参模型漏字段导致刷新后入口消失。理由见 D-18 |
| 第十六批 16.7 | 人物渲染视图复用可编辑卡 | 弹窗那张卡抽成 `CharacterFormCard`（含文档↔表单互转），文件面板宿主 `CharacterDocForm` 绑缓冲区、保存走 `useFiles.save` 一条口；只读副本 `CharacterDocCard` 与其六条 CSS 删除；顺带修一处既存特异性 bug（`.character-modal input` 把视觉隐藏的 file input 撑成 808×34）；真机改值→源码 bullet 同步→保存→改回，服务器逐字节还原 |
| 第十六批 16.9 | 拖到极限关栏后拖回来 | `applyPane` 改成可逆且不再结束手势（只有 `pointerup` 结束）；两处坑记档：拖拽闭包读的是旧 `hidden` 快照（改 `hiddenRef` + 函数式写入）；`closePane` 复位宽度会让方向键同向再按被误判成「变宽」而重开（已隐藏时基准换成 `CLOSE_AT-1`）；真机按住鼠标来回拖两条边界都过 |
| 第十六批 16.8 | 无专属渲染视图不给切换钮 | `hasRenderedView()` 一条判据；通用 `MarkdownText` 覆盖层与 `.file-rendered` 删除，第十四批那条 `inset: 0` 断言改钉现存两个覆盖层；顺带量出 16.11（`views[draft]` 两个写者互相覆盖，责任提交 `6b6dbd3`），只记录不顺手改 |
| 第十八批·前几轮 | 设置页改成列表 + 入口（点了 3 次） | `groups` 一张表 + 左 `tablist` + 右唯一 `tabpanel`，加设置项=添一行；自己刚造出的面板 `<h2>` 重复被当场删掉（§0.7 条六），改 `aria-labelledby` 指回列表；实测 nav 196px / panel 776px、切换后屏上分组名只出现一次；窄屏转横排 |
| 第十八批 18.2 | 工具轨迹改成动作条目 | `traceActions()` 容错解析三种模型方言（含库里 message 20 的 MiniMax 原文），每步行 = 图标 + 中文动作名 + 等宽参数 chip，无框无底色；认不出的名字保留原拼写、解析不出的行退回原文；**「状态点」这条做不到就登记成 18.3 待点头，不假装做到**（`ok` 没落库，刷新后无从判断） |
| 第十八批 18.1 | 思考过程与正文要有字体区分 | 上一轮我只量了边框/底色没量字体就写了「已做」——实测思考文字与回答完全相同（`.chat-card p` 0,1,1 盖掉容器上的 0,1,0）。修法：样式挂到 `<p>` 本身 + 选择器抬到 (0,2,1)（平手按源码顺序，本块在上必输；与 4.2 压 CodeMirror 同一课）。实测 12px/italic/--text-2 vs 正文 13px/normal/--text-1 |
| 第十九批 19.2 | 多供应商 + 任务路由 + 预留生图 | `llm.providers`(JSON) + `llm.route.<task>`，**无建表无迁移**；`resolve_settings(session)` 不传 task 时行为不变，故 status／allowed_models／6 处测试替身一行没改；`RoutedLLMClient` 预建每供应商客户端并按 task 挑；UI 成「默认供应商 + 供应商列表 + 任务路由表」，生图行明写未启用。真机加第二个供应商把审稿路由过去、验证解析后**删净 `llm.*` 还原为测试前的 0 行**；顺带把 conftest 的测试引擎暴露成 `db_engine`（此前新测试会误读真库） |
| 第十九批 | 主人 3 条新批注 | 19.1 附件与 19.2 多供应商**已获主人 2026-09-06 批准**，按「勾完即删该条」从 UI-BACKLOG 删除（结论就是下面两行）；19.3 外观完善仍在做 |
| 第十九批 19.1 | 附件按钮不能用 + chip 呈现 | 附件变成 `ContextBlock(kind=附件, TIER_CORE)` 追加进 `writing_context.selected`，**没开第二条注入通路**（D-04/D-06，理由见 D-19）；chip 按主人画的形状、发送成功才清空；边界全部带原因。铁证：上传一个只存在于临时目录的文件，模型回答里引了其中唯一那句「三律刻在石壁上每一寸都磨得发亮」 |
| 第十六批 16.2 | 切换钮图标方向 | 图标改成说「当前所在」、文案仍说「目标」；两条标签条的钮抽成 `components/ViewToggle.tsx` 一个组件，分叉从结构上不可能；真机往返四次映射一致 |
| 第十六批 16.4 | 树搜索搜不到屏上的章号 | `chapterNumberLabel()` 一处出显示值、`chapterMatches()` 用它判定并进单测；真机 `0`/`00`/`0001` 全通；设定库那条核查后**本来就正确**，未动 |
| 第十六批 | 主人 9 条新批注 | 9 条全部落地并逐条真机验证；**主人 2026-09-05 认可后按「勾完即删该条」从 UI-BACKLOG 删除**，结论就是本表上面那 9 行；该批遗留的 16.10／16.11 两条「本轮没做」仍在 UI-BACKLOG 第二节 |
| 前几轮遗留 | 四条 bar 收窄并统一 | 实测台阶：顶栏 48（工作台与书架**本来就相同**）、两条标签条 38 = 38（`.file-tabs` 原 40）、工具栏 33（原 44，比标签条还高，与「略窄」相反）。真因是唯一的方框主按钮 `事实落库` 保留全局 `min-height:32px + padding:6px`，在一排 28px 里高出 7px 把整条顶到 40——方框按 §0.6 保留，只把高度对齐。`uiInvariants` 28 → 29 块钉住这四个数 |
| 16.11（登记于第十七批） | 深链进 draft.md 时钮与画面相反、第一次点击没反应 | `views[draftPath]` 的两个写者都出自我的 `6b6dbd3`：`open()` 写 true、`hydrateChapterDraft()` 无条件写 false，谁后跑谁赢。改成**由决定栏位的地方写标志**——`WorkbenchPage.markProseStage` 紧挨着 `setRightView` 的那两处调用，hydrate 不再碰它。真机四条路径全验：深链／首屏／点章名／点 draft.md 行，标志与画面一致且一次点击即换画面；新增 1 条 jsdom 回归测试 |
| 16.10（登记于第十五批之后） | 代码块复制钮底边被 `<pre>` 压住 | **登记时写的 6px 是错的**：那是审计 `STEP=2` 的采样假象，逐行复测真实重叠只有 1 行（y=453）。真因是控制行 29px（28+1px 上边框）里 28px 的钮末行正好落在与代码体的接缝上、被后到的静态 `<pre>` 盖住；给控制行 `position:relative; z-index:1` 解决，不动 `pre`。把工具的假警报当成产品缺陷写进台账，是「先量再写」没做到底 |
| 前几轮遗留 | 未配置提示在教主人做错事（第九批开的） | 文案改成指向 `/settings` 并可点跳转。**改前先核实新断言是否为真**：`get_llm_client` 每请求 `resolve_settings(session)` 读 `app_config`，所以「保存即生效、不用重启」成立（D-16）。顺带修同一条全局病根：`button{min-height:32px}` 把句内链接撑到 32px、把横幅从 35px 顶到 49px——用本项目既有的伪元素扩热区补回 29×25 命中，视觉仍是 24×18。新增 1 条前端测试钉死「不许再出现改 .env / 重启后端的指令」 |
| 第十七批 17.3 | 空态加书本图标 | `editor-empty` 补 `BookOpen 22`、`library-empty` 补 `Users 24`，装饰图标统一 `aria-hidden`；整类扫出六个面板级空态并逐个用断言钉住（列表内一行提示刻意不加，否则成装饰）；顺带发现 `--text-3` 是从未定义的悬空 token |
| 第十七批 17.2 | 空态中心不在一条线上 | 量出来两行彼此是对齐的，偏的是**整块**：按整栏居中而正文列是「整栏 − 56px 缩略栏」，恒定右偏 28px；同类四张脸（空态／目录表格／人物卡）一起改到 1174.5，用 `--minimap-w` token 而非字面量；顺带清掉从未定义的悬空 token `--text-3` |
| 第十七批 17.1 | 思考过程不要方框、不要一两字一行 | `reasoningParagraphs()` 按「片段中位数 ≤4 即判为被 delta 撕碎」在**读出时**拼回一段（库里数据不改写）；思考体换成无框无底色的 `.chat-thinking-body` + `<p>`，等宽方框只留给工具轨迹；真机展开旧碎行实测 1 段 137 字 4 行、border 0、background 透明 |
| 第十七批 | 主人 3 条新批注（思考过程呈现／空态两行不同轴／空态缺图标） | 判据已写进 UI-BACKLOG 第一节；17.2 与 17.3 是同一处缺陷的两面（`.editor-empty` 少了 §0.6 要求的图标，居中基准因此与文件栏不一致）|

