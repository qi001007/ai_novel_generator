# UI 待清账（勾完即删该条）

职责边界：本文件**只记未完成的界面条目**。理由在 `DECISIONS.md`，进度史在
`WORKSTREAM-PLAN.md`，视觉规格在 `UI-DESIGN.md`。做完一条就把结论写成
`WORKSTREAM-PLAN.md` 的「已经能用的 / 还差什么」里改一句，然后**把整条从本文件删掉** -
不要在这里堆历史。过程细节不用抄：每条批注的提交信息里都带着实测数字，
`git log --oneline` 与 `git show <提交>:docs/UI-BACKLOG.md` 随时能翻回来。
处理闭环（先写文件→按严重度一条一条做→真机截图→同批改断言→单独提交）在
`AGENTS.md`《UI 批注处理闭环》。

**开工前必读的三条纪律**（每一条都是踩出来的）：

0. **主人新提的任何东西，先写进本文件，再动手；一条都不留在对话里。**
   2026-09-05 主人原话：「你先把这几轮没弄的需求都给我记到 MD 文件里面去，
   **不要再在这里靠记忆了**。」同一轮里他还说过「我每次跟你提了之后，你老是经常容易忘记」。
   记忆不是存储，对话必被截断。落不进文件的请求等于没提。
1. **效果类改动必须真机截图、自己看过**才算完成。只看 diff 不算。
2. **「我怀疑的那个东西不存在」≠「主人看到的东西是什么」**。前一条让我把同一个问题
   答错三次（行号与正文之间那条线），因为我只证明了边框是 0px，从没去枚举接缝上
   到底站着哪些元素。量法见文末「取证脚本」。

---

## 〇、2026-09-06 第二十四轮批注（3 条界面 + 1 项新功能 + 3 项登记）

主人这一轮同时给了三样东西：三条界面批注、一个明确的新功能（导出正文）、四个关于
架构的问题。他还说了两句要紧的话：**「我看了一下你还在做的清单，写得不错，这些我认可」**
（`WORKSTREAM-PLAN §三` 的顺序照旧，伏笔墙/地图仍在最后）与**「需要我拍板的都交给你」**
（延续上一轮授权，架构问题的裁定见 `DECISIONS.md` **D-26**，不再回来问）。

做单顺序按严重度：正在用的删除弹窗 → 对话 chip 压缩 → 调用详情展开区样式 → 导出（要后端配合）→ 三项登记。

### 24.1 删除作品弹窗：去掉「把书名原样打一遍」，只留一次确认（批注 1）

- 主人：「这个弹窗不用那么详细，还要我输入书名确认。我右键哪本书点删除，肯定是指删除
  对应的这本书，不要加让我输入名字这种操作。**但是你可以保留确认的过程**……
  你可以弹窗提示『是否确认删除』，而且不要留那么多字」
- 判据：
  1. 弹窗里**没有输入框**：`.book-delete` 内的 `input` 数量 = 0；可操作控件只有「取消 / 删除」两枚；
  2. 说明文字一句话，**不含书名的字数 ≤ 16**（现在是 62 字 + 「输入书名确认」标签），
     不写「没有回收站」「请把书名原样打一遍」这类提示语；
  3. 确认过程保留：点删除按钮之前不发请求；点「取消」或 Esc 或遮罩 → 不发请求；
  4. 删除仍只走那一条 `DELETE /api/novels/{id}`（D-23），成功后书卡消失、不新增第二条通路；
  5. 防误按：默认焦点在「取消」，Enter 不触发删除；
  6. 断言搬家：`uiInvariants` 现在钉着 `confirmTitle !== deleteTarget.title`（第 684 行），
     同一个提交里改成钉「弹窗内不许出现输入框、说明文字 ≤16 字」——**不许只删不钉**；
  7. 测试：`BookshelfPage.test.tsx` 那条「asks for the title before it deletes」改写成
     「打开即可删、取消不删、Enter 不删」三条断言；
  8. 真机截图看过（弹窗态 + 删除后书架）。

- **已做（2026-09-06 真机浅色 + 深色各看过一次，两张截图自己看过）**。
  · 弹窗从「62 字说明 + 输入框 + 标签」缩成「一行 7 字 + 两枚按钮」：真机量得
    `input` **0** 个、`label` **0** 个、可操作控件只有取消 / 删除；盒子 560×204 → **400×188**
    （内容缩完，560px 的空盒子比长说明更难看，宽度改成贴着内容走）。
  · 门槛没了，误按的防线换成**焦点**：打开即聚焦「取消」，真机按 Enter → 弹窗关闭、
    书架 6 本一本没少；Esc 同样只关窗；点「取消」不发请求。删除仍只走那一条
    `DELETE /api/novels/{id}`（D-23），成功后卡片消失。
  · 第十六批那条「界面提示语不许用句号」的断言**当场拦下了我写的「删除后无法恢复。」**
    ——它扫的是整类文案而不是当初点名的两处。文案改成无句号。记一笔：这是断言第一次
    替主人改文案，不是第一次替我改，说明那条闸门是有用的。
  · 断言搬家（同一条提交里）：`uiInvariants` 钉 `confirmTitle !== deleteTarget.title`
    那条改成钉「不许出现『输入书名确认』+ 说明必须就是这七个字」；断言块数不变。
  · 测试：`BookshelfPage.test.tsx` 原「打字才能删」改写成「打开即可删 / 取消不删 /
    Enter 不删 / 删完卡片消失」四段；菜单项提示从「输书名确认」改成「不可恢复」。

### 24.2 对话里的注入 chip：合并到不随章数增长（批注 3）

- 主人：「1. 合并全书蓝图：『全书蓝图点主线』和『全书蓝图点主题』完全可以归成一类；
  2. 简化 B 层目录：直接写成一个方框，比如『第一章-第九章』；
  3. 压缩第一章-简报：到时候写到一两百章，难道你要贴 100 多个字词条在这儿吗？」
- 判据：
  1. 同一份文件的多节**并成一枚** chip（`A 全书蓝图 · 5 节`），不再一节一枚；
  2. 按章号排的资料（B 目录 / D 简报 / 正文）压成**范围** chip：连续写 `第1–9章`，
     断号写 `第1–3、7–9章`；
  3. **可测量的主判据**：真机 novel 5 那条「引用 29 份资料」的回复，chip 枚数 29 → **≤ 10**；
  4. **线性增长测试**：单测喂 100 条 `brief:1..100` + `toc:1..100`，断言渲染出的 chip 枚数
     与喂 9 条时相同（不随章数增长）；
  5. 压缩不许丢信息：chip 的 `title`（悬停）仍是完整清单，层序仍是 A→B→C→D→正文→设定→附件（§23.3 不回退）；
  6. 分组与范围**只写在 `contextLayers.ts` 一处**，`ChatPane` 与调用详情共用，不许出现第二份；
  7. 真机截图看过（压缩前后的展开态各一张）。

- **已做（2026-09-06 真机六条回复全量过，截图看过）**。
  · 真机 novel 5 那条「引用 29 份资料」的回复：**29 枚 → 8 枚**，两行排完（原来是七行）。
    实测八枚依次是 `A 全书蓝图 · 5 节 / 作品信息 / B 目录 · 第1–9章 / C 剧情弧 · 碑鸣（1-3） /
    D 简报 · 第1–9章 / 正文 · 第 1 章 未命名 / 伏笔 · 2 节 / 附件 · 附件样本.md`。
    同一页另五条回复也量了：3 枚、3 枚、5 枚、7 枚、6 枚，没有一条回退到逐节一枚。
  · **主判据落成测试**：`frontend/src/contextLayers.test.ts` 喂 100 章的 toc + brief，
    chip 枚数与 9 章时相同（都是 3 枚），文案 `第1–100章`；断号那条测 `第1–3、7–9章`。
  · fixture 是真数据不是编的：29 条 refs 取自 `chat_message id=31`、18 条 blocks 取自
    `generation_run id=15`，用只读 SQL 原样导出后贴进测试。**顺手补上第二十三批 §23.5 判据 4
    欠的那条**（「18 行收敛成 10 行」当时只写在文档里、没进测试），现在钉住了。
  · 压缩不丢信息：每枚 chip 的 `title` 仍是它盖掉的原文列表（真机量到 B 目录那枚悬停 9 行、
    蓝图那枚 5 行）；「引用 29 份资料」那句仍报真实份数，压缩只改画几个框。
  · 一处定义：`compressRefs` 与层序、分组键同在 `contextLayers.ts`。调用详情那张表
    **仍是一行一份文件**（点开要逐节看原文，并成一行就没法展开）- 摘要面与检视面共用同一份
    规则表，但各自决定压不压，这不是第二处真源。
  · 层序不回退：测试断言 chip 的层秩单调不减（A→B→C→D→正文→设定→附件）。

### 24.3 调用详情的展开区：一份文件多节不许再堆成一片（批注 2）

- 主人：「你这里的形式很不好看，显得很杂乱……A 层级全本蓝图是直接把所有的内容堆在一起。
  重新设计一种好看的样式，让这种堆叠不显得重复和密集，避免产生视觉疲劳」
- 密集的来源（我先量出来，再改）：`blueprint.md` 展开时**每一节**都重复
  「A 全书蓝图 ·」这个前缀、每一节都重复「以下就是模型当时读到的内容」、每一节各套一个盒子。
- 判据：
  1. 展开区顶部只出现**一次**文件名与状态（`blueprint.md · 5 节 · 132 字 · 已交给模型`），
     下面每节的标题只剩小节名（`主线`），不再带层与前缀；
  2. 那句「以下就是模型当时读到的内容」出现次数 5 → **1**（整区一条，不逐节重复）；
  3. 视觉层级：小节标题一行（左标题、右字数），原文用一条左细线 + 正常行高的引用块，
     节与节之间靠间距分开，**每节不再各自带边框盒子**；
  4. 长原文（> 6 行）默认截断到 6 行并给「展开/收起」，短的一律全显 —— 密集感一半来自长段落；
  5. 未交给模型的那一节仍看得见、仍标「未交」（§23.5 判据 5 不回退）；
  6. 真机截图：`blueprint.md`（5 节）与单节文件（作品信息）各一张，自己看过；
  7. 命中区审计 0 small / 0 clipped / 0 unreachable。

### 24.4 导出正文：全书 / 单章 / 单份 draft.md 三个口（新功能）

- 主人：「现在没连发布平台（比如起点），所以要能导出正文：
  ① 全书导出做到首页图书的右键菜单里；② 单章导出做到文件树每个章节的右键里；
  ③ 每一个 `draft.md` 也应该有右键打开器，可以选择导出，格式是 MD 文件」
- 我的裁定（不再回来问）：导出是**读**，不开第二条写通路；正文由后端从 DB 现拼，
  前端只负责触发下载 —— 因为「全书按章顺序拼」这件事只有真源那边说得准。
- 判据：
  1. 后端新增**只读**端点 `GET /api/novels/{id}/export?scope=book|chapter&chapter_number=N&format=txt|md`，
     带 `Content-Disposition`；`git diff` 里不许出现任何新的写端点；
  2. 三个入口：书架书卡右键 →「导出全书…」「导出全书 Markdown」；文件树章节节点右键 →
     「导出本章…」「导出本章 Markdown」；`draft.md` 文件节点右键 →「导出为 Markdown」；
  3. 文件名实测：`观星残卷_全书.txt`、`观星残卷_第0001章.md` 这种带书名与章号的形态，
     中文文件名在 Edge 下不乱码（真机验）；
  4. 内容口径：txt = `第N章 标题` + 空行 + 正文 + 两个空行；md = 保留原 markdown；
     **空正文的章跳过**，全书字数 = 各章 `word_count` 之和（后端测试比对，不许目测）；
  5. 一本书一章都没有 / 章号不存在 → 明确报错（404），前端显示错误，**不许下载 0 字节文件**；
  6. 只读性有测试钉住：导出前后同一章的 `updated_at` 与 `content` 不变；
  7. 测试：后端 ≥ 4 条（全书 txt、单章 md、空章跳过、只读性），前端 ≥ 3 条（三个菜单项存在 + 点击打的是那个 URL）；
  8. 真机截图：两个右键菜单各一张 + 下载到的文件用文本打开核对首行。

### 24.5 登记：备份与误删找回（主人的第三个问题，本轮只登记）

- 主人：「你有没有做备份设计？就是如果我误删了数据，还能找回的那种设计」
- 现状（如实）：**没有**。删除是物理删除、一条事务、14 张表（D-23），无回收站。
  而且 24.1 把输入门槛去掉之后，误删的门槛更低了 —— 这两条是耦合的，所以我照做，
  但把备份提到 `WORKSTREAM-PLAN §三` 的**下一步第 1 位**（伏笔墙往后让）。
- 判据：`WORKSTREAM-PLAN §二` 新增一行「备份/找回」写清现状与方案（SQLite 文件级快照：
  启动时 + 每次删除前复制一份带时间戳的 `.db`，保留最近 N 份），`§三` 顺序表相应改动。
  本轮不写实现代码。

### 24.6 登记：内存与存储增长（主人的第四个问题）

- 主人：「我担心这个应用以后运行起来会比较吃内存……一般都是日志和其他东西比较吃存储，
  相关的优化可以加进剩下还没做的条目里面」
- 实测（2026-09-06，本机 `backend/novel_generator.db`）：**424 KB**；按表看最大文本列的字节合计 ——
  `chat_message` 43.8 KB、`generation_run` 34.0 KB、`chapter` 5.0 KB。
  **他的判断是对的**：正文不是最大头，对话记录与调用记录才是（两者约占 78/85 KB）。
- 判据：`WORKSTREAM-PLAN §二` 新增一行，列出四条具体可做的收敛（`chat_message` 与
  `generation_run` 的 excerpt/全文保留上限、调用记录按书分页而不是一次全取、
  前端文件层 entries 的缓存上限、日志文件轮转），并写明「不做向量库、不做后台常驻任务」这条边界。
  本轮不写实现代码。

### 24.7 裁定：`.md` 投影层留不留（主人的第一个问题）

- 主人：「你这个 MD 文件是虚假的，真实存储不是这个样子……你觉得『正文正常渲染、草稿用
  MD 文件呈现』这一层转换有没有必要？还是草稿也不用 MD 直接渲染？或者你有更好的方案？」
- 判据：裁定写进 `DECISIONS.md` **D-26**，理由要能反驳自己（为什么不是「删掉这层」），
  并落到界面上一句人话（`WORKSTREAM-PLAN §一` 改一句：文件栏是「文档视图」，不是磁盘目录）；
  同时在对话里用大白话回答，不许用编号黑话。本轮不实现新东西。

---

## 一、未结的界面条目（按主人点名次数排序）

第二十、二十一轮的拍板项已由主人整体授权给我，裁定见 `DECISIONS.md` **D-22**：
对比度三处已修（`--on-accent` / `--on-danger` 一个 token 管整类，四组配色逐元素复量全 0）、
顶栏方框统一成无边框图标钮、删除语义做两条真端点、`required_facts` 保留机械口径并把
契约写到看得见的地方、S0 在隔离临时库跑 `--live`、生图继续明写未启用。

- [ ] **`UI-DESIGN §0.1` 那张表有三个值和代码不一致**（19.3 顺手发现，未改）：
      表里 dark `surface #1C1C1E`、`surface-alt #26262A`、light `text-2 #73716C`，
      代码是 `#1f2023`、`#191a1b`、`#6d6b66`（后两处正是第十轮批注 1 与 `--text-2` 那一次修定的）。
      这是文档漂移不是界面缺陷，但它是「文档说的和跑着的不是一回事」。
- [ ] **`arcs.md` 在调用详情里叫「分卷」，在树和文档里叫「剧情弧」**（23.5 顺手发现）：
      同一份资料两个名字，读清单的人会以为是两样东西。改一处词表即可，本轮未做。
- [ ] **伏笔墙 / 地图面板接上真实数据源**：面板内**零请求**，是未接线，不是没数据（别再重复排查）。
      **2026-09-05 主人明确排到最后：「伏笔墙跟地图的数据接线往后靠，先把其他的做了」**

---

- [ ] **F5 后停在正文面时，选中的是章本身而不是文件**（21.1 顺手记下，本轮故意不做）：章的恢复走工作台 store 的既有路径，把 `chapter` 也塞进 `workbench.stage` 会造出第三个「谁在台子上」的持有者。

## 二、被后端语义卡住的（不要在前端臆造第二条写通路）

- [ ] 树右键「重命名 / 删除」：**仍然未开放**。章号是主键（D-11），删一章/插一章要回答
      「后面的章号要不要顺延」，那是 D-13 未决的产品语义。界面上继续 `disabled` + 原因。
- [ ] `dbg`（id 6）与 `MD探针`（id 3）两个误建作品：端点已就绪（D-23），
      **我没有替主人删** - 他在书架上右键、把书名打一遍即可。

---

## 三、取证脚本（每轮 UI 改动后跑一遍，别靠目测）

放在 `.scratch/`（已 gitignore，改动不入仓库，结论写进文档）：

- `cdp.mjs` → `connect(url)` 返回 `{ evaluate, click, clickPoint, send, shot, close }`；
  无 Chrome，用裸 Edge + CDP（端口 9333）。
- **写 CDP 脚本一律 `-Encoding utf8`**。用 `ascii` 会把脚本里的中文静默变成 `?`，
  于是 `find(n => n.getAttribute("aria-label") === "展开调用记录")` 永远匹配不上，
  报出来的「控件不存在」全是脚本 bug——本会话被它误导三次。
- **命中区审计**：`node .scratch/hit-area-audit.mjs [url] [open-file|appearance]`。
  它**不信 CSS 盒子**（上一轮就是盒子骗的我：`.tree-prefix` 写着 28px，实际被 overflow
  裁掉），而是用 `elementFromPoint` 从控件中心向四向走，量「真点得到」的范围。
  三段输出：`small`（命中 < 24px）、`clipped`（命中框任何一部分落在最近滚动/裁切容器之外）、
  `unreachable`（中心被别的东西盖住）。改完任何一处控件尺寸都跑一遍。
- **对比度审计**：逐元素算合成背景与前景的 WCAG 比值（浅色主题曾一次抓出 13 处不过 AA）。
- **量「主人看到的线」的正确方法**：给定一条视觉接缝，枚举 `elementFromPoint` 沿途
  以及两侧容器**所有子元素**的 `left/right/backgroundColor`，不要只查你怀疑的那个元素有没有 border。
- **改字号之后**：`#root` 上有 zoom，指针像素 ≠ CSS 像素，任何把指针变成坐标或宽度的地方
  必须过 `toCssPx()`；量法是「拖 80px 看栏宽是不是正好走 80px」「右键菜单的左上角与
  点击点 gap 是否为 0」。

---

## 四、门禁（每次推送前必须全绿）

```
cd E:\novel-generator\backend;  .venv\Scripts\python.exe -m pytest -q
cd E:\novel-generator\frontend; npm run test -- --run
# 期望条数只写在 HANDOFF.md 的门禁段（同一数字抄两处，必有一处是错的 - 已错过三次）
cd E:\novel-generator\frontend; npx tsc -b --force --pretty false            # 必须 clean，--force 别信增量
cd E:\novel-generator\frontend; npm run build                                # 必须 clean
cd E:\novel-generator\.scratch; node hit-area-audit.mjs <url>                # 0 small / 0 clipped / 0 unreachable
git -c http.proxy=http://127.0.0.1:7890 push origin main
```

测试要**跟着设计改断言**，不许删。`WorkbenchLayout.test.tsx` 里那条把「按两次方向键」
写成 `300 + 32` 的算式而不是字面量——上次它绑死 312，因为默认值变了而以错误的原因失败。
