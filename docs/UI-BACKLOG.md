# UI 待清账（勾完即删该条）

职责边界：本文件**只记未完成的界面条目**。理由在 `DECISIONS.md`，进度史在
`WORKSTREAM-PLAN.md`，视觉规格在 `UI-DESIGN.md`。做完一条就勾掉并在
`WORKSTREAM-PLAN.md` 写一行结论，不要在这里堆历史。

**开工前必读的三条纪律**（每一条都是踩出来的）：

0. **主人新提的任何东西，先写进本文件，再动手；一条都不留在对话里。**
   2026-09-05 主人原话：「你先把这几轮没弄的需求都给我记到 MD 文件里面去，
   **不要再在这里靠记忆了**。」同一轮里他还说过「我每次跟你提了之后，你老是经常容易忘记」。
   记忆不是存储，对话必被截断。落不进文件的请求等于没提。
1. **效果类改动必须真机截图、自己看过**才算完成。只看 diff 不算。
2. **「我怀疑的那个东西不存在」≠「主人看到的东西是什么」**。前一条让我把同一个问题
   答错三次（行号与正文之间那条线），因为我只证明了边框是 0px，从没去枚举接缝上
   到底站着哪些元素。量法见文末「取证脚本」。

---


## 一、2026-09-05 第十八轮批注（1 条）+ 排序变更

主人 2026-09-05 原话：「伏笔墙跟地图的数据接线往后靠，先把其他的做了」
→ 下面第二节的顺序照此调整：**伏笔墙／地图面板接数据源排到最后**。

### 18.1 ✅ 思考过程的字体与大小要和正文区分开（批注 1）

- 主人：「这个思考过程里的字体和大小应该和正文的字体有区分，你可以采用这样的形式来呈现（如图）。
  当你运行的是一些命令的时候，你可以采用这种形式（如图）」，两张参考图：
  思考 = 斜体、更小、更淡的成段文字；命令 = 图标 + 动作名 + 参数 chip 的缩进条目。
- **这条是第十七批 17.1 的续账，而且暴露了我上轮没做到底的地方**：我只量了
  `border`／`background`／`white-space`，**没量字体**，就在台账里写了「已做」。
  实测真相：`.chat-card p { font-size: 13px; color: var(--text-1) }`（特异性 0,1,1）
  盖掉了我写在 `.chat-thinking-body`（0,1,0）上的 `12.5px / --text-2`——
  思考文字与回答文字 computed **一模一样**（13px、rgb(236,235,233)、normal）。
- 判据（这次把字体本身量进去）：
  1. `.chat-thinking-body p` 实测 `font-size` 小于正文 `.chat-md p` 的 13px、
     `color` 为 `--text-2`、`font-style: italic`；三项都要用 computed 值报数，不看 CSS 源码；
  2. 思考与回答在**同一张卡里**并排时肉眼可分（截图自己看过）；
  3. 断言进 `uiInvariants`：思考段的字号/颜色/斜体必须挂在能压过 `.chat-card p` 的
     选择器上——挂在容器上等于没写。
- **已做（2026-09-05 真机 computed 值量过、截图看过）**。两刀：
  ① 样式挂到 `<p>` 本身而不是容器；② 选择器抬到 `.chat-card .chat-thinking-body p`
  （0,2,1）——因为 `.chat-card p` 也是 (0,1,1)，**平手时按源码顺序**，而本块在它上面，
  所以第一版只有 `italic` 生效（那条不冲突）、字号颜色照旧被盖。与第十五批 4.2
  压 CodeMirror 自带描边是同一课：**靠特异性压，不靠顺序**。
  实测：正文 `13px / normal / rgb(236,235,233)`；思考 `12px / italic / rgb(157,155,150)`。
  截图看过，与主人的「深度思考」参考图同形（更小、更淡、斜体、无框、正常成段）。
  中文斜体是浏览器合成的伪斜体，若他觉得难看，删 `font-style` 一行即可——
  字号与色阶本身已经足够区分，这点写在注释里。
  `uiInvariants` 29 → **30** 块（钉三项 computed 级属性挂在能压过的选择器上，
  并钉正文仍是 13px，防止有人反过来把正文改小来「制造区分」）。

### 18.2 ✅ 工具轨迹改成「图标 + 动作 + 参数 chip」的条目（批注 1 的后半）

- 现状：`.chat-trace-body` 是一个等宽方框，里面是 `step 1: read_file(path=arcs.md) -> ok`
  这种日志行——既不是主人给的「命令」形式，也把参数原文摊在界面上。
- 判据：去掉方框；每步一行 = 动作图标 + 中文动作名 + 参数 chip（如 `arcs.md`）；
  成功/失败用状态点而不是 `-> ok` 文本（§0.7 条四「状态用圆点与颜色」）；
  命中区审计里这些 chip 不是可点控件（`aria-hidden` 或非交互元素），不新增死控件。
- **已做（2026-09-05 真机展开存库那条 MiniMax 消息量过、截图看过）**。
  `traceActions()`  tolerant 解析三种方言（JSON / Ruby 式 `tool => … --path "…"` / 裸名），
  每步行 = 图标 + 中文动作名（`ACTION_LABELS`：读取／写入／列出文件／检索，
  **认不出的名字保留原拼写**，不编中文标签）+ 等宽参数 chip；无框无底色。
  **解析不出动作的行退回原文块**——宁可难看，不能把跑过的东西说错。
  实测：`[TOOL_CALL] {tool => "read_file", args => {--path "settings/foreshadow.md"}}`
  → 一行「📄 读取 `settings/foreshadow.md`」，`.chat-trace-list` 的 `border 0px`、
  `background 透明`。新增 4 条解析测试，其中 MiniMax 那条**用的是库里 message 20 的原文**。
  `uiInvariants` 30 → **31** 块；前端 151 → **155 passed / 19 files**。
- **一处判据我做不到，改记为待办而不是假装做到**：原写「成功/失败用状态点」。
  实时事件里 `ok` 是有的（`event.data.ok`），但**没有落库**——`chat_message` 不存每步结果，
  所以刷新后同一条轨迹无从判断成败。要做成状态点得先给轨迹加持久化（新列或新表），
  那是数据通路的事、不是样式的事。**已登记为 18.3，等主人点头**；
  现在条目不带状态点，也不写 `-> ok` 那种文本，避免刷新前后两种样子。

### 18.3 轨迹的成败要落库才能画状态点（新登记，等主人点头）

- [ ] 现状：`event.data.ok` 只活在流式过程中，`chat_message` 不存每步结果 → 刷新后无从判断。
      要做 §0.7 条四的「状态用圆点」，得先给轨迹加持久化（新列或新表 + 迁移 + 出参），
      属新增数据通路，**与 D-17 ② 那次「等点头」同类**，故单独一条不顺手做。
      判据（若批准）：每步的 `ok` 落库；刷新前后轨迹条目显示同一个状态点；
      失败步的圆点用 `--danger`、成功用 `--success`，不出现 `-> ok` 文本。

---

## 二、前几轮欠着未做的（按主人点名次数排序）

[x] ~~章节编辑器标签条也要有同一个切换钮~~（已做，见 §1.4 / §3.4：钮在
      `.editor-tabs-actions`，状态就是 `useFiles.views[path]`，投影全文由 `ensure()` 取）。
- [x] ~~人物卡片的长字段目前是只读预览，没有「渲染 ↔ 原文件」这一对~~
      （已做，见 §3.1：整份 `settings/characters/N.md` 的渲染视图就是人物卡片，
      四个长字段都在卡上，钮与文件条共用同一个 `views[path]`）。
- [ ] **设置页改成「列表 + 入口」的可拓展结构**（主人点了 3 次）。
      现在整页都是「模型接入」，主人明确：以后要加更多设置项，必须预留结构。
- [ ] **色系切换**（含**代码配色**与**字体切换**）。主人说现在这套字「复古、像上个世纪网页文字」；
      另外早前就要求**新增一套蓝色系**。
- [ ] **书卡右键菜单**：更换封面 / 删除 / 其他改动收进右键（主人：「按钮能用图标就用图标」）。
- [ ] **封面色改成调色盘**（或至少加一个调色盘入口）。
- [x] ~~拖动分栏边界到极限应能关闭该栏~~（已做，见上面 §2.2，本条从第四节划掉）。

- [ ] **对比度剩两处（品牌色决定，需主人拍板）**：`--accent` 压自己的淡色底 4.29；
      深色主题白字压 `--accent` 3.31。

---

- [ ] **伏笔墙 / 地图面板接上真实数据源**：面板内**零请求**，是未接线，不是没数据（别再重复排查）。
      **2026-09-05 主人明确排到最后：「伏笔墙跟地图的数据接线往后靠，先把其他的做了」**

---

## 三、被后端语义卡住的（不要在前端臆造第二条写通路）

- [ ] 树右键「重命名 / 删除」：全仓除 `chat.py` **无任何 delete 路由**。
      与 **Q-07**（反馈记录进不进文件层）、**Q-09**（人物删除必然 405）、「删除作品」是**同一条决策**，一起定。
- [ ] 书架上误建的 `dbg`（novel id=6）删不掉，同因。

---

## 四、取证脚本（每轮 UI 改动后跑一遍，别靠目测）

放在 `.scratch/`，已验证可用的入口：

- `cdp.mjs` → `connect(url)` 返回 `{ evaluate, click, clickPoint, send, shot, close }`；
  无 Chrome，用裸 Edge + CDP（端口 9333）。
- **写 CDP 脚本一律 `-Encoding utf8`**。用 `ascii` 会把脚本里的中文静默变成 `?`，
  于是 `find(n => n.getAttribute("aria-label") === "展开调用记录")` 永远匹配不上，
  报出来的「控件不存在」全是脚本 bug——本会话被它误导三次。
- **命中区审计（已写）**：`node .scratch/hit-area-audit.mjs [url] [open-file]`。
  它**不信 CSS 盒子**——上一轮就是盒子骗的我（`.tree-prefix` 写着 28px，实际被 overflow
  裁掉）——而是用 `elementFromPoint` 从控件中心向四向走，量「真点得到」的范围，
  伪元素覆盖层和负 margin 都算进去。三段输出：`small`（命中 < 24px）、
  `clipped`（命中框任何一部分落在最近滚动/裁切容器之外）、
  `unreachable`（中心被别的东西盖住）。改完任何一处控件尺寸都跑一遍。
- **对比度审计**：逐元素算合成背景与前景的 WCAG 比值（浅色主题曾一次抓出 13 处不过 AA，
  其中十处共用同一个 `--text-2`）。
- **量「主人看到的线」的正确方法**：给定一条视觉接缝，枚举 `elementFromPoint` 沿途
  以及两侧容器**所有子元素**的 `left/right/backgroundColor`，不要只查你怀疑的那个元素有没有 border。

---

## 五、门禁（每次推送前必须全绿）

```
cd E:\novel-generator\backend;  .venv\Scripts\python.exe -m pytest -q
cd E:\novel-generator\frontend; npm run test -- --run
cd E:\novel-generator\frontend; npx tsc -b --pretty false
cd E:\novel-generator\frontend; npm run build
git -c http.proxy=http://127.0.0.1:7890 push origin main
```

测试要**跟着设计改断言**，不许删。`WorkbenchLayout.test.tsx` 里那条把「按两次方向键」
写成 `300 + 32` 的算式而不是字面量——上次它绑死 312，因为默认值变了而以错误的原因失败。
