# UI 待清账（勾完即删该条）

职责边界：本文件**只记未完成的界面条目**。理由在 `DECISIONS.md`，进度史在
`WORKSTREAM-PLAN.md`，视觉规格在 `UI-DESIGN.md`。做完一条就把结论写成
`WORKSTREAM-PLAN`《四、历史》一行，然后**把整条从本文件删掉**，不要在这里堆历史。
处理闭环（先写文件→按严重度一条一条做→真机截图→同批改断言→单独提交）在
`AGENTS.md`《UI 批注处理闭环》。

**开工前必读的三条纪律**（每一条都是踩出来的）：

0. **主人新提的任何东西，先写进本文件，再动手；一条都不留在对话里。**
   2026-09-05 主人原话：「你先把这几轮没弄的需求都给我记到 MD 文件里面去，
   **不要再在这里靠记忆了**。」同一轮里他还说过「我每次跟你提了之后，你老是经常容易忘记」。
   记忆不是存储，对话必被截断。落不进文件的请求等于没提。
1. **效果类改动必须真机截图、自己看过**才算完成。只看 diff 不算。
2. **「我怀疑的那个东西不存在」≠「主人看到的东西是什么」**。前一条让我把同一个问题
   答错三次（行号与正文之间那条线），因为我只证明了边框是 0px，从没去枚举接缝上
   到底站着哪些元素。量法见文末「取证脚本」。

---

## 〇、2026-09-06 第二十三轮批注（5 条 + 文档收口）

主人这一轮还有一句比批注更要紧的话：**「你让我拍板的这些我基本都看不懂，在 DOS 里找项目进度
会很迷茫，文件太多了」**。所以本批除了 5 条界面账，还包含 §23.6 文档收口 -
**做完之后进度只看一份文件：`docs/PROGRESS.md`**。

### 23.1 右键菜单的「删除作品」整条被颜色糊死，且不看色系（批注 1）

- 主人：「这个按钮的颜色已经完全覆盖了它的字体，我什么都看不清。而且它没有根据我现在
  选择的色系进行切换：我选择了蓝色系，但是它还是橙色」
- 根因（我自己上一轮造的，`921fca1`）：我给那条菜单项加了 `tree-menu-item danger` 两个类，
  而全局 `button.danger` 是 `background: var(--danger)`。`.tree-menu-item.danger` 只写了
  `color`，没写 `background` - 于是**底色来自全局、字色来自我**，两者都是 `--danger`，
  字就没了。**这正是 T-19 的同一条坑，我在记下它之后的第二个提交里又踩了一遍。**
- 判据：
  1. 菜单项不许有底色：`.tree-menu-item.danger` 自己声明 `background: none`，
     悬停才给一层淡底；深浅两色 × 两色系四张截图各看过一次，字必须读得出来；
  2. 断言从「只钉 primary」扩成**钉住整类**：任何 `.tree-menu-item.*` 都必须自带 background；
  3. 色系那条按事实回答：破坏性动作的红是**语义色**，不跟色系换（跟了就会出现在
     「删除」长得像主行动的情况）；但菜单里**选中/悬停**的颜色必须跟色系，实测切蓝后
     悬停底与勾都是蓝。把这条写进 UI-DESIGN，别只写在对话里。

### 23.2 树右键里「在其后新建章节」与第一项功能重叠（批注 2）

- 主人：「这个和第一个『新建下一章简报』不是功能重叠了吗？要么合并，要么删掉」
- 判据：删掉「在其后新建章节」这一条（它是 disabled 的占位，而第一项已经在做同一件事的
  可用版本）。菜单条目从 5 条减到 4 条；「重命名 / 删除」保持未开放（D-13 未决）。
  断言钉住：树菜单里不许同时出现两条「新建…章节/简报」。

### 23.3 对话里的注入清单要按层归堆（批注 3）

- 主人：「要以 A 层跟 A 层在一起、B 层跟 B 层在一起、C 层跟 C 层在一起的形式。
  你现在就很乱，我不好看到底注入了哪些」
- 判据：`.chat-refs` 的 chip 顺序改成 **A → B → C → D → 正文 → 设定 → 附件 → 其余**，
  同层内保持原有顺序（稳定排序，不许按字数或字母乱排）；顺序表只有一处定义，
  与 §23.4 的清单共用同一份，不许两处各写一遍。

### 23.4 调用详情的注入清单要按 ABCD 排（批注 4）

- 主人：「同样，这里层级的排序尽量是按 ABCD 归类，而不是 A、D、C 设定，或者又 D 又 B」
- 判据：清单分组后的**组顺序**用 §23.3 同一份层序表；实测 run 15 从
  `A 作品信息 / A 蓝图 / D 简报 / C 弧 / 设定 / D 简报 / B 目录…` 变成
  `A → B → C → D → 设定`。

### 23.5 注入清单要以「文件」为一行，不是以一节为一行（批注 5）

- 主人：「全本蓝图里有好几个内容其实都是同一个文件里的，比如主线、主题、核心冲突。
  你完全可以把它们整合在一起…现在这样不好看，最好以文件的形式来呈现。
  这样我点预览，就可以在下面看到里面的主线、主题和核心冲突。
  要是以后上下文注入变多起来了，你这个岂不是会非常非常长？」
- 现状取证（真读 run 15 的清单）：18 行里 `ref=blueprint:3` 占 5 行（主线/主题/核心冲突/
  约束/终局），`ref=toc:6/7/8` 3 行、`ref=foreshadow:1/2` 2 行、`ref=setting:2/3` 2 行 -
  它们**确实来自同一份文件**。而 `ref=brief:7/8/9` 是 `chapters/000{1,2,3}/brief.md`
  **三份不同文件**，不能并成一行。
- 判据：
  1. 一行 = 一份文件。分组键按文件层真实形状来：`blueprint / toc / arc / foreshadow /
     setting / worldview` 是「一份文件多节」→ 按 kind 归；`brief / character / chapter /
     novel / attachment` 是「一条 ref 一份文件」→ 按 ref 归。**这张表只有一处定义**；
  2. 组行显示：层 · 文件名 · N 节 · 合计字数 · 交付状态（混合时写「3 节已交 · 2 节未交」）；
  3. 点一行 = 展开这份文件的**每一节**（各自标题、字数、原文摘录），不是只看其中一节；
  4. 18 行必须收敛到 **9 行**（run 15 实测：作品信息、blueprint.md、toc.md、arcs.md、
     三份 brief.md、characters/6.md、foreshadow.md、worldview.md 归并后的真实数）；
     条数写进测试，不许靠目测；
  5. 「未交给模型」的行仍要看得见（预算挤掉的东西必须能查），折叠状态不许把它藏没；
  6. 老记录（无结构化清单）仍走现在那段纯文本回退，不许白屏；
  7. 真机截图：run 15 的清单折叠态 + 展开 blueprint.md 的那态，各看过一次。

### 23.6 文档收口：进度只看一份（主人的第三条要求）

- 主人：「1. 哪些已经做了，哪些没做，还有哪些是要做的？2. 接下来你要做哪一步、
  什么时候能做完？3. 已经做过且没必要保留的，可以删掉」
- 判据：
  1. 新建 `docs/PROGRESS.md` - **大白话**，不许出现 S0/S2/U7/D-22/Q-07 这类编号黑话，
     要用「正文生成能跑通」「人物删除按钮以前点了报 405」这种人话；三段：
     已经做到什么程度 / 还差什么 / 下一步做哪个 + 预计；
  2. `WORKSTREAM-PLAN.md` 与 `REQUIREMENTS.md` **删除**，未结内容逐条搬进 PROGRESS
     （老规矩：未结项一条都不许丢，搬完再删）；两份里的历史结论进 `DECISIONS`；
  3. `HANDOFF.md` 的「读哪份文件」表改成指向新集合，并写明「**只想知道进度就看 PROGRESS.md**」；
  4. 删完做一次交叉引用扫描：全仓不许有指向已删文件的链接；
  5. 文档总大小从 164.8KB 再降，且 PROGRESS.md 一屏能读完当前状态。

---

## 〇、2026-09-06 第二十一轮批注（1 条）

主人：「需要我拍板的都交给你，自己结合实际状态、技术方案、UI美观决定」——
§一 里那四条待拍板项的裁定见 `DECISIONS.md` **D-22**，不再回来问。

### 21.1 ✅ 离开工作台再回来，右栏被换掉（批注 1）

- 主人：「我进入这个页面之前，是 draft.md 的那个页面，但是我退出之后，它就会自动切成
  渲染过后的正文页面。我要的是我进来是什么样，出来就是怎么样的」
- 判据：
  1. 在 draft.md 的**源码面**去 `/settings` 再返回 → 仍是源码面；在**正文面**去回 →
     仍是正文面；两个方向各验一次，不许只修一边；
  2. 换一本书再换回来，各书回到自己离开时那一面（不是全局一个值）；
  3. 刷新浏览器（F5）同样保持 - 与已有的栏宽／隐藏栏两个偏好同一口径；
  4. 深链 `?file=` 与 `?chapter=` 仍然优先于「上次那一面」（显式意图大于历史状态）；
  5. **不许新增第二个真源**：这个事实今天只有 `rightView` 一个持有者，持久化它不等于
     再造一个和 `useFiles.active`/`views` 打架的影子状态；
  6. 测试：返回后仍是源码面 / 换书各自记住 / 深链覆盖记忆 三条；
  7. 真机走一遍并截图（这条是「效果类」，不看 diff 不算完）。

- **已做（2026-09-06 真机六条路径全过，截图看过）**。
  · 根因不是 store 被清（`attach()` 有 `novelId === novelId && metas.length` 的早退，
    站内返回时文件层根本没动），而是 **`rightView` / `railPage` 是带死默认值的 React state**：
    重挂即回到 `"editor"`，于是文件栏被换成正文面。修法与旁边的栏宽／隐藏栏同一口径：
    一本书一条记录存 `localStorage["workbench.stage"]`。
  · 真机六条：源码面 →站内去回 ✓ / →F5 ✓（连文档都重开，tab 与钮的文案都对）；
    正文面 →站内去回 ✓ / →F5 ✓（截图：仍是 0001 章正文、1241 字、工具条齐）。
  · 两个反例是真机量出来的，不是想出来的：
    ① 恢复效果排在写入之后 → F5 后 `file` 已被冲成 `null`；改成恢复在前、
      且未恢复不许写；② 无条件重开文档 → 离开时是**正文面**的人被 `open()`
      钉回源码面（draft.md 的 open 本来就 stamp 源码），于是加一条
      「只有离开时确实是文件栏才重开」。
  · 一处**已知不做**（诚实记录）：F5 后停在正文面时，选中的是章本身而不是文件，
    章的恢复走的是工作台 store 的既有路径；把 `chapter` 也塞进这条记录会造出
    第三个「谁在台子上」的持有者，不值。
  · 测试：`WorkbenchLayout` +2（去回与 F5 都保持同一面 / 显式深链大于记忆）、
    `uiInvariants` 35 → **36** 块。前端 182 → **185 passed / 20 files**。
    （两处数字第一版写成 36→37 与 182→184：又是没当场量。`git show <提交>:文件` 数
    `it("` 才是 35→36；测试跑完的计数是 185。T-21 同一条，第二次犯。）

---

## 一、未结的界面条目（按主人点名次数排序）

第二十、二十一轮的拍板项已由主人整体授权给我，裁定见 `DECISIONS.md` **D-22**：
对比度三处已修（`--on-accent` / `--on-danger` 一个 token 管整类，四组配色逐元素复量全 0）、
顶栏方框统一成无边框图标钮、删除语义做两条真端点、`required_facts` 保留机械口径并把
契约写到看得见的地方、S0 在隔离临时库跑 `--live`、生图继续明写未启用。

- [ ] **`UI-DESIGN §0.1` 那张表有三个值和代码不一致**（19.3 顺手发现，未改）：
      表里 dark `surface #1C1C1E`、`surface-alt #26262A`、light `text-2 #73716C`，
      代码是 `#1f2023`、`#191a1b`、`#6d6b66`（后两处正是第十轮批注 1 与 `--text-2` 那一次修定的）。
      这是文档漂移不是界面缺陷，但它是「文档说的和跑着的不是一回事」。
- [ ] **伏笔墙 / 地图面板接上真实数据源**：面板内**零请求**，是未接线，不是没数据（别再重复排查）。
      **2026-09-05 主人明确排到最后：「伏笔墙跟地图的数据接线往后靠，先把其他的做了」**

---

## 二、被后端语义卡住的（不要在前端臆造第二条写通路）

- [ ] 树右键「重命名 / 删除」：**仍然未开放**。章号是主键（D-11），删一章/插一章要回答
      「后面的章号要不要顺延」，那是 D-13 未决的产品语义。界面上继续 `disabled` + 原因。
- [ ] `dbg`（id 6）与 `MD探针`（id 3）两个误建作品：端点已就绪（D-23），
      **我没有替主人删** - 他在书架上右键、把书名打一遍即可。

---

## 三、取证脚本（每轮 UI 改动后跑一遍，别靠目测）

放在 `.scratch/`（已 gitignore，改动不入仓库，结论写进文档）：

- `cdp.mjs` → `connect(url)` 返回 `{ evaluate, click, clickPoint, send, shot, close }`；
  无 Chrome，用裸 Edge + CDP（端口 9333）。
- **写 CDP 脚本一律 `-Encoding utf8`**。用 `ascii` 会把脚本里的中文静默变成 `?`，
  于是 `find(n => n.getAttribute("aria-label") === "展开调用记录")` 永远匹配不上，
  报出来的「控件不存在」全是脚本 bug——本会话被它误导三次。
- **命中区审计**：`node .scratch/hit-area-audit.mjs [url] [open-file|appearance]`。
  它**不信 CSS 盒子**（上一轮就是盒子骗的我：`.tree-prefix` 写着 28px，实际被 overflow
  裁掉），而是用 `elementFromPoint` 从控件中心向四向走，量「真点得到」的范围。
  三段输出：`small`（命中 < 24px）、`clipped`（命中框任何一部分落在最近滚动/裁切容器之外）、
  `unreachable`（中心被别的东西盖住）。改完任何一处控件尺寸都跑一遍。
- **对比度审计**：逐元素算合成背景与前景的 WCAG 比值（浅色主题曾一次抓出 13 处不过 AA）。
- **量「主人看到的线」的正确方法**：给定一条视觉接缝，枚举 `elementFromPoint` 沿途
  以及两侧容器**所有子元素**的 `left/right/backgroundColor`，不要只查你怀疑的那个元素有没有 border。
- **改字号之后**：`#root` 上有 zoom，指针像素 ≠ CSS 像素，任何把指针变成坐标或宽度的地方
  必须过 `toCssPx()`；量法是「拖 80px 看栏宽是不是正好走 80px」「右键菜单的左上角与
  点击点 gap 是否为 0」。

---

## 四、门禁（每次推送前必须全绿）

```
cd E:\novel-generator\backend;  .venv\Scripts\python.exe -m pytest -q      # 200 passed
cd E:\novel-generator\frontend; npm run test -- --run                        # 182 passed / 20 files
cd E:\novel-generator\frontend; npx tsc -b --force --pretty false            # 必须 clean，--force 别信增量
cd E:\novel-generator\frontend; npm run build                                # 必须 clean
cd E:\novel-generator\.scratch; node hit-area-audit.mjs <url>                # 0 small / 0 clipped / 0 unreachable
git -c http.proxy=http://127.0.0.1:7890 push origin main
```

测试要**跟着设计改断言**，不许删。`WorkbenchLayout.test.tsx` 里那条把「按两次方向键」
写成 `300 + 32` 的算式而不是字面量——上次它绑死 312，因为默认值变了而以错误的原因失败。
