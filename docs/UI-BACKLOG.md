# UI 待清账（勾完即删该条）

职责边界：本文件**只记未完成的界面条目**。理由在 `DECISIONS.md`，进度史在
`WORKSTREAM-PLAN.md`，视觉规格在 `UI-DESIGN.md`。做完一条就勾掉并在
`WORKSTREAM-PLAN.md` 写一行结论，不要在这里堆历史。

**开工前必读的三条纪律**（每一条都是踩出来的）：

0. **主人新提的任何东西，先写进本文件，再动手；一条都不留在对话里。**
   2026-09-05 主人原话：「你先把这几轮没弄的需求都给我记到 MD 文件里面去，
   **不要再在这里靠记忆了**。」同一轮里他还说过「我每次跟你提了之后，你老是经常容易忘记」。
   记忆不是存储，对话必被截断。落不进文件的请求等于没提。
1. **效果类改动必须真机截图、自己看过**才算完成。只看 diff 不算。
2. **「我怀疑的那个东西不存在」≠「主人看到的东西是什么」**。前一条让我把同一个问题
   答错三次（行号与正文之间那条线），因为我只证明了边框是 0px，从没去枚举接缝上
   到底站着哪些元素。量法见文末「取证脚本」。

---

## 一、2026-09-05 第十五轮批注（3 条 11 件事 + 补 2 件 = 13 件，全部未动代码）

主人 2026-09-05 02:5x 原话：「动手之前，同样把这些需求都写进文件里，你之后才知道哪些做了、
哪些没做」。所以下面每一条都带**判据**，做完在条首打 ✅ 并写实测数字，没做完不许划掉。
上一轮（第十四批）我声称给章节标签条加了滚轮与滑块（提交 `42ec581`），主人本轮第 1 条
前两点就是「你这里根本没改」——**先实测再声明**，见 §1.1/§1.2。

### 1 章节标签条（`.editor-tabs`）四件事

- [x] **1.1 滚轮不能左右滚**。判据：鼠标停在标签条上滚滚轮，`scrollLeft` 必须变。
      **已做（2026-09-05 真机滚过）**：右栏 558px 时 `scrollLeft 0→49`、366px 时 `49→209`；
      无溢出时滚轮不动页面（`pageY 0`、正文 `scrollTop 0`）——无事可做就别乱动。
- [x] **1.2 悬停不出现移动滑块**，与文件条不一致。判据：悬停时 `.h-scroll` 存在且
      `opacity` 由 0→1，拖动可改 `scrollLeft`，且 tab 高度不被压。
      （第十四批批注 4 的同一个要求，文件条做到了、章节条没做到 = 又没扫完同类。）
      **已做**：悬停 `opacity: 1`、thumb 宽实测 457px→174px（随溢出比例缩），
      拖动后 `scrollLeft → 241`；`stripH 38`、`tabH 31` 全程不变（没被压）。
      **根因不是「滑块没做」，也不是我上一轮写进本文件的「收缩地板太高」**：
      `.editor-pane` 是 grid 项而隐式列按内容定宽，7 个标签时它宽 **938px**、装在
      **768px** 的右栏里——标签溢出的是**右栏**（被页面裁掉），不是自己的 scroller，
      所以 `scrollWidth == clientWidth == 882`，滚轮与滑块永远无事可做。
      修的是容器：`.editor-pane { grid-template-columns: minmax(0, 1fr); min-width: 0; }`
      （`.file-editor` 本来就有 `min-width: 0`，这才是两条条真正不一致的那一处）。
      顺带对齐该对齐的数：`.editor-tab` 上限 `240px` → `15rem`（=文件条的 210px），
      现在两条条 min 77px / max 210px / `flex: 0 1 auto` 三个数一模一样（实测已核）。
- [x] **1.3 文件条上 draft.md 的切换钮「点了没作用」**。主人要的不是"就地排版"，
      而是**切到正文页**（见 §3.2）。判据：在文件编辑器打开 `chapters/0001/draft.md`
      点钮 → 出现的是**正文编辑视图**，再点回来。
      **已做（与 §1.4 同一次改动，2026-09-05 真机点过）**：draft.md 的渲染视图 = 正文页，
      不再是就地 `MarkdownText`。实测 `.right-column` 从 `file-editor` 变 `editor-pane`、
      `.file-rendered` 覆盖层对 draft 不再出现、往返一次后 `aria-label` 回到「切到源码视图」。
- [x] **1.4 章节标签条后面也要有同一个切换钮**，能从正文切回 draft.md 源码。
      判据：`.editor-tabs` 里有 `.editor-tabs-actions`，`aria-label` 随状态变化，
      且它与文件条读写**同一个** `useFiles.views[path]`（不是第二处状态）。
      **已做**：钮在 scroller 之外（与文件条同一条 CSS：`.file-tabs-actions,
      .editor-tabs-actions`），两条条都只调 `toggleViewLabel(path, views)` /
      `isSourceView(path, views)` —— 文案与状态各只有一个出处。
      实测：正文页 stripH 38 / tabH 32（加钮前后不变，tab 没被压）、
      钮的 `aria-label` 「切到源码视图」→ 点 → 文件条「切到正文页」→ 点 → 回正文页；
      命中区审计两种台面上都是 0 small / 0 clipped / 0 unreachable。
      防再犯：`uiInvariants.test.ts` 新增一条，钉「两条条共用一个函数、
      任何一侧都不许再长出自己的 `sourceView` state、不许硬编码『切到渲染视图』」。

### 2 编辑页拖窄后的两件事

- [x] **2.1 工具栏控件被推出右边界直接消失**（「机械校验 / AI 自检 / 通过终审 / 打回 /
      事实落库」）。判据：把右栏拖到 400px 宽，这些动作**一个都不许消失**——要么换行、
      要么收进一个 `…` 溢出菜单；不允许 `overflow:hidden` 吃掉。
      **已做（2026-09-05 真机量过，选了「换行」这条）**。拖到 400px 时六个动作
      `visible/hit` 全 true（工具栏 2 行 73px）；300px 时 3 行 103px 仍全 true。
      量程里挖出**两个更狠的同类**，都是「控件在、点不到」：
      ① **窗口变窄不会重排**：`.workspace` 只有最后一列是 `1fr`，对话栏留着宽窗口时的
      像素值，正文列被挤到 **24px**，六个动作 `visible:false / hit:false` 全灭
      （这才是主人看到「直接消失」的现场）。加了 resize 时按同一规则重新夹紧；
      ② **网格项的自动最小尺寸是内容**：`.chat-messages` 在 214px 的栏里实测 **328px**，
      溢出去盖住正文列，`elementFromPoint(AI 自检)` 回的是 `div.chat-messages`。
      `.chat-pane`／`.chat-messages`／`.chat-dock` 一并补 `min-width: 0`
      （与 §1.1/§1.2 那条 `.editor-pane` 同族，这次一类扫完）。
      正文列的底线 `EDITOR_MIN = 160`：放不下就按 §2.2 关掉这一列，绝不留一条裁切的工具栏；
      地板也不再能盖过天花板（`min(CHAT_MIN, max)`，原来 400 的地板在窄窗口里反过来压垮正文列）。
      顺带修了审计脚本自己的假警报：最后一行 `brief.md` 压在视口底边（y1010 > 1000），
      走不到完整尺寸就被记成 `small 121x21`——现在单列 `edge` 一类报出来，不静默掩盖。
- [x] **2.2 拖到极限应当关闭该栏**（规划栏 / 对话页 / 编辑页三栏都要）。
      **这条主人反复提过，已在清单里躺了很久**（原第四节「拖动分栏边界到极限应能关闭该栏」）。
      判据：把 splitter 拖到 < 阈值（建议 90px）→ 该栏隐藏，顶栏对应图标变成未按下；
      再点图标可恢复原宽度。三栏行为一致。
      **已做（2026-09-05 真机三段长拖）**：阈值 90px，指针与方向键走同一条 `applyPane`。
      实测 1656px 窗口：结构栏拖过头 → `hidden=sidebar`、轨道 `44px 542px 1px minmax(0,1fr)`
      （不留孤儿 splitter）、图标 `aria-pressed=false`、点图标回到 **300px（拖之前的宽度）**；
      对话栏边界向右压过正文 → `hidden=editor`、图标未按下；边界拖到左边 → `hidden=chat`。
      两处顺手补的坑：① 关闭必须**幂等**——第 12 次 ArrowRight 会把刚关掉的栏又「关」回来
      （toggle 语义），真机连拖三段也验过不再回弹；② 正文栏回来时不能是 113px 的窄条
      （实测值），所以取消隐藏时向对话栏要回 `EDITOR_MIN 420px`。
      拖动过程中地板降到 90（不是静止的 260/400），列会**看得见地塌到边**再消失；
      静止与从存储恢复仍用原 min。

### 3 「渲染 ↔ 原文件」必须是 VS Code 那种双向关系（主人给了明确类比）

- [x] **3.1 人物 `.md` 的切换钮要切到「人物卡片渲染页」**，不是 markdown 排版。
      判据：打开 `settings/characters/6.md` 点钮 → 出现人物卡片视图，再点回源码。
      **已做（2026-09-05 真机点过，就用主人点名的那份）**：新增 `CharacterDocCard.tsx`
      （`parseCharacterDoc` 读结构行了 4 个长字段），渲染视图与目录列表同一规矩
      `inset: 0` 盖住 body；`showRendered` 明确排除人物路径，所以它不会再被排版抢掉。
      实测：卡片 `560x346`、姓名「沈砚舟」、徽标「主角团」、`1 - 12 章`、
      四个字段 身份/目标/行为约束/当前状态 齐全，结构行没漏上屏（`leakedHeader:false`）；
      点钮 → 卡片消失、`aria-label` 变「切到人物卡片」；再点 → 回到卡片。
      老规矩也复验：**未保存的字照样上卡片**（在源码里敲「未保存也得上卡片」后切回卡片可见），
      测完已把 buffer 还原、服务器那份 `len 210 / rev cc375383eb90` 未被写入。
      分级色与人物库共用同一个 `LEVEL_LABELS`（从 CharacterLibrary 导出，不留第二份表）。
- [x] **3.2 `draft.md` 的切换钮要切到「正文渲染页」**（章节编辑器那个正文）。
      **已做**（同 §1.3）：`renderedViewLabel()` 按 kind 选渲染视图——
      `toc.md`→列表视图、`settings/characters/N.md`→人物卡片、`draft.md`→正文页、其余 md→渲染视图。
      §3.3 已把两边并成一份 buffer，剩下的就是让钮把右栏在「正文页 ↔ draft.md 源码」之间搬。
      实测记一笔：现在从树里点 `draft.md` 会经 `open()` 把右栏切到文件页，而点章节号切回正文页
      时 `openChapterTab → hydrateChapterDraft` 只 seed 空槽、不覆盖已有 buffer（这是 §3.3 的正确行为）。
- [x] **3.3 draft 与正文必须同步**（主人：「draft 变，正文也变」）。**已做，2026-09-05 真机量过**：
      `chapterDrafts` / `draftContent` / `draftSaved` 三个字段从 workbench 删除，唯一 buffer 是
      `useFiles.entries["chapters/NNNN/draft.md"]`；正文 = `draftBody()` 投影（剥掉标题行与规则行），
      `draftDocument()` 是它的逆，两者按字节互逆（新增一条 round-trip 测试钉住，含「末尾回车不被吃掉」）。
      实测作品 5 第 4 章：源码页敲 `源码侧Y2` → 正文页立刻同字（`len 5`）；正文页敲 `正文侧Y1` →
      源码页两条都在；一次 Ctrl+S 后服务器文件 `len 48` 含两条、DB 章节行 `word_count 12` 同步、
      三处 dirty 点同时归零（tab / 树行 / 文件 tab 各 0）；刷新后正文页从文件层读回 `len 12`、
      结构行不上屏、dirty 点 0。写通路仍是同一条 `PUT /api/novels/{id}/files/{path}`（D-01 未动）。
      顺带修掉两处同族的丢字面：`closeTab` 不再删 buffer；`selectNovel` 现在 reset 文件层
      （`chapters/0001/draft.md` 这个路径在两本书里是同一个字符串，不清就是拿甲书的字充乙书）。
      **已知根因**：现在是**两份 buffer**——`useWorkbench.chapterDrafts[id].draft`
      与 `useFiles.entries["chapters/NNNN/draft.md"].draft`，两者写同一个后端投影，
      所以会互相覆盖。**这是真 bug，不是观感问题。**
      判据：在文件条源码里改一句话 → 正文页立刻同字；在正文里改 → 源码页立刻同字；
      两处 dirty 状态一致（树行、标签、文件 tab 三处同步）。
      正解：`draft.md` 的 entry 成为唯一 buffer，正文 = 它的投影（去掉结构行），
      章节编辑器不再自己存一份。写通路仍只有 `PUT /api/novels/{id}/files/{path}`（红线 D-01/D-02）。
- [x] **3.4 渲染页也要有切回去的钮**（双向）。判据：任一渲染视图里都能找到同一个钮
      切回源码，`aria-label` 与状态一致。**已做**：正文页（章节条）、目录列表视图与
      其余 md 的排版覆盖层（文件条）都挂着同一个钮、读同一个 `views[path]`；
      人物卡片那一处随 §3.1 一起接。

**本轮开工前实测（1570px 视口，作品 5，点开 7 个章节标签）**：
`.editor-tabs` 只有一个子元素 `.editor-tabs-scroll`；7 个 tab 各 124px、
scroller `scrollWidth 882 == clientWidth 882` → **没有溢出**，所以滚轮与覆盖式滑块
都无事可做（`thumb: false`、滚轮后 `scrollLeft: 0`）。这解释了主人在 8 个 tab 且
把右栏拖窄之后看到的现象：tab 先被 `min-width: 5.5rem` 收缩，**收缩到地板才溢出**，
而主人期望的是"溢出后能滚、有滑块"。→ §1.1/§1.2 的真实待办不是"滑块没做"，而是
**章节标签的收缩地板太高 / 与文件标签不一致，导致该溢出的时候不溢出**。
  【做完时更正这句诊断】两条条的 `min-width` 本来就都是 5.5rem、都 `flex: 0 1 auto`，
  地板不是原因；真因是容器少一条有界的 grid 列（实测数字见上面 §1.2）。
同时实测确认：**`actions: false` —— 章节标签条上根本没有切换钮（§1.4 确实没做）**，
上一轮我只在文档里承认"仍欠一半"，没有把钮做出来。

**本轮结束时（上下文耗尽前）的状态**：§1.1 / §1.2 / §1.3 / §1.4 / §2.1 / §2.2 /
§3.1 / §3.2 / §3.3 / §3.4 / §4.1 / §4.2 **全部未做**，一条都没提交代码。
本轮唯一的提交是这份记录（`a25597b` 起）。**下一个会话请从 §3.3（两份 buffer 互相覆盖，
是真数据 bug）开始做**，它比观感问题严重；其次是 §1.4+§3.4（同一个钮、同一处状态，
出现在两条标签条上），再做 §1.1/§1.2（把章节标签的地板与文件标签对齐，让该溢出时溢出），
然后 §2.2（拖到极限关栏，主人反复提过的一条老账）、§2.1（工具栏溢出收纳）、
§3.1（人物卡片作为 `characters/*.md` 的渲染视图）、§4.1+§4.2（聚焦不画框，一类扫完）。

### 4 正文编辑区聚焦后那圈框，整个删掉（主人 2026-09-05 03:0x 补）

- [ ] **4.1** 点击正文编辑区（`章节正文` textarea）后，四周出现一圈**浅色框线**，主人要求删掉。
      **根因是我上一轮自己造的**：第十四批批注 5 我把「聚焦上主色」改成中性
      `--text-2`（深色主题里 rgb(157,155,150) ≈ 浅灰白），于是它从橙框变成了白框——
      **换了颜色没换存在**。判据：点进正文区，`.editor-scroll` 与 textarea 的
      `border-top-color` 在聚焦前后**完全不变**（保持 `transparent`），`box-shadow: none`，
      `outline-width: 0`。
- [ ] **4.2 同类一次扫完**（这条教训我已经挨过四次）：所有「聚焦才出现的框/底」的
      编辑面都要按同一规则处理，不许只改正文这一处 ——
      `.editor-body:focus-within .editor-scroll`、`.file-cm` 聚焦态、`.chat-input textarea`、
      `.composer:focus-within`、`.tree-search:focus-visible`、`.toc-search:focus-within`、
      `.toc-row input/textarea:focus`、`.character-modal` 三件套、`.prefs-field input`。
      统一成：**聚焦只允许「光标 + 极轻的边框提亮到 `--border-strong`」，不允许出现
      一圈独立描边、不允许 `box-shadow` 描边、不允许主色**。
      判据（写进 `uiInvariants.test.ts`）：任何含 `focus` 的规则都不得出现
      `outline: <非 0>` 或 `box-shadow: 0 0 0 Npx` 或 `var(--accent)`；
      键盘可达性靠 `:focus-visible` 的**内容变化**（如 caret、字重），不靠画框。

---

**统一规则（写下来免得又各做一套）**：一个文档 = 一份 buffer + 一个 `views[path]` 布尔 +
**一个按钮**，按钮出现在该文档所在的**每一条标签条**上；渲染视图按 kind 选择
（`toc.md`→表格，`characters/*.md`→人物卡片，其余 md→`MarkdownText` 排版，
`draft.md`→正文编辑视图）。

---

## 一、上一轮（第十四批）遗留的两条

- [ ] **章节编辑器标签条的切换钮**（已并入上面 §1.4 / §3.4，不再单列）
- [ ] **人物卡片长字段的「渲染 ↔ 原文件」**（已并入上面 §3.1，不再单列）

## 二、上一轮（第十三批）留下的 2 条

- [ ] **每条 AI 回复都要有思考过程可展开**（主人：「正常来说你所有的对话都有思考过程」）：
      工具轨迹的前端折叠已做（`chat-trace`）。但**模型自己的推理文本仍拿不到**——
      `services/llm.py:222` 只读 `delta.content`，全仓 `reasoning` 0 命中。
      要做得动后端：读 `delta.reasoning_content` → 存进消息行（加列 + 迁移）→
      `/chat` 出参带上 → 前端折叠。**等主人点头再动，这是新增数据通路。**
  - [ ] 你对AI回复的页面呈现的效果应该是有的题目该放大一点就放大一点，正文小一点。你要有像 MD 文件那样的排版效果，而不是单纯地把文字放在那
- [ ] **`[TOOL_CALL]` 历史脏数据删不删**：消息 id=20 存于 09-04 05:04，方言过滤修复
      `93ec9ee` 是同日 13:06，属历史行；前端已折叠不再上屏。删它属于删除语义那条决策。

---

## 三、第九批改完之后新开的（1 条，2026-09-04）

> 另两条由第十批的颜色改动一并解决：活动条改到了与侧栏同档，
> 弹层用的 `--surface` 现在比列底高一级，不再往下沉。

- [ ] **未配置提示的文案在教主人做一件错事**：`.chat-notice` 写「在 backend/.env 填入密钥后
      重启后端」，而 D-16 之后配置的真源是 `app_config` 表、入口在 `/settings`。
      一句话改口的活，第九批没顺手做（不在批注范围内）。

---

## 四、前几轮欠着未做的（按主人点名次数排序）

[x] ~~章节编辑器标签条也要有同一个切换钮~~（已做，见 §1.4 / §3.4：钮在
      `.editor-tabs-actions`，状态就是 `useFiles.views[path]`，投影全文由 `ensure()` 取）。
- [x] ~~人物卡片的长字段目前是只读预览，没有「渲染 ↔ 原文件」这一对~~
      （已做，见 §3.1：整份 `settings/characters/N.md` 的渲染视图就是人物卡片，
      四个长字段都在卡上，钮与文件条共用同一个 `views[path]`）。
- [ ] **设置页改成「列表 + 入口」的可拓展结构**（主人点了 3 次）。
      现在整页都是「模型接入」，主人明确：以后要加更多设置项，必须预留结构。
- [ ] **色系切换**（含**代码配色**与**字体切换**）。主人说现在这套字「复古、像上个世纪网页文字」；
      另外早前就要求**新增一套蓝色系**。
- [ ] **书卡右键菜单**：更换封面 / 删除 / 其他改动收进右键（主人：「按钮能用图标就用图标」）。
- [ ] **封面色改成调色盘**（或至少加一个调色盘入口）。
- [x] ~~拖动分栏边界到极限应能关闭该栏~~（已做，见上面 §2.2，本条从第四节划掉）。
- [ ] **四条 bar 收窄并统一**：`.file-tabs` / `.editor-tabs` 同宽，`.editor-toolbar` 略窄，
      `.topbar` 与首页顶栏同高。
- [ ] **对比度剩两处（品牌色决定，需主人拍板）**：`--accent` 压自己的淡色底 4.29；
      深色主题白字压 `--accent` 3.31。
- [ ] **伏笔墙 / 地图面板接上真实数据源**：面板内**零请求**，是未接线，不是没数据（别再重复排查）。

---

## 五、被后端语义卡住的（不要在前端臆造第二条写通路）

- [ ] 树右键「重命名 / 删除」：全仓除 `chat.py` **无任何 delete 路由**。
      与 **Q-07**（反馈记录进不进文件层）、**Q-09**（人物删除必然 405）、「删除作品」是**同一条决策**，一起定。
- [ ] 书架上误建的 `dbg`（novel id=6）删不掉，同因。

---

## 六、取证脚本（每轮 UI 改动后跑一遍，别靠目测）

放在 `.scratch/`，已验证可用的入口：

- `cdp.mjs` → `connect(url)` 返回 `{ evaluate, click, clickPoint, send, shot, close }`；
  无 Chrome，用裸 Edge + CDP（端口 9333）。
- **写 CDP 脚本一律 `-Encoding utf8`**。用 `ascii` 会把脚本里的中文静默变成 `?`，
  于是 `find(n => n.getAttribute("aria-label") === "展开调用记录")` 永远匹配不上，
  报出来的「控件不存在」全是脚本 bug——本会话被它误导三次。
- **命中区审计（已写）**：`node .scratch/hit-area-audit.mjs [url] [open-file]`。
  它**不信 CSS 盒子**——上一轮就是盒子骗的我（`.tree-prefix` 写着 28px，实际被 overflow
  裁掉）——而是用 `elementFromPoint` 从控件中心向四向走，量「真点得到」的范围，
  伪元素覆盖层和负 margin 都算进去。三段输出：`small`（命中 < 24px）、
  `clipped`（命中框任何一部分落在最近滚动/裁切容器之外）、
  `unreachable`（中心被别的东西盖住）。改完任何一处控件尺寸都跑一遍。
- **对比度审计**：逐元素算合成背景与前景的 WCAG 比值（浅色主题曾一次抓出 13 处不过 AA，
  其中十处共用同一个 `--text-2`）。
- **量「主人看到的线」的正确方法**：给定一条视觉接缝，枚举 `elementFromPoint` 沿途
  以及两侧容器**所有子元素**的 `left/right/backgroundColor`，不要只查你怀疑的那个元素有没有 border。

---

## 七、门禁（每次推送前必须全绿）

```
cd E:\novel-generator\backend;  .venv\Scripts\python.exe -m pytest -q
cd E:\novel-generator\frontend; npm run test -- --run
cd E:\novel-generator\frontend; npx tsc -b --pretty false
cd E:\novel-generator\frontend; npm run build
git -c http.proxy=http://127.0.0.1:7890 push origin main
```

测试要**跟着设计改断言**，不许删。`WorkbenchLayout.test.tsx` 里那条把「按两次方向键」
写成 `300 + 32` 的算式而不是字面量——上次它绑死 312，因为默认值变了而以错误的原因失败。
