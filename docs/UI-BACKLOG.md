# UI 待清账（勾完即删该条）

职责边界：本文件**只记未完成的界面条目**。理由在 `DECISIONS.md`，进度史在
`WORKSTREAM-PLAN.md`，视觉规格在 `UI-DESIGN.md`。做完一条就勾掉并在
`WORKSTREAM-PLAN.md` 写一行结论，不要在这里堆历史。

**开工前必读的三条纪律**（每一条都是踩出来的）：

0. **主人新提的任何东西，先写进本文件，再动手；一条都不留在对话里。**
   2026-09-05 主人原话：「你先把这几轮没弄的需求都给我记到 MD 文件里面去，
   **不要再在这里靠记忆了**。」同一轮里他还说过「我每次跟你提了之后，你老是经常容易忘记」。
   记忆不是存储，对话必被截断。落不进文件的请求等于没提。
1. **效果类改动必须真机截图、自己看过**才算完成。只看 diff 不算。
2. **「我怀疑的那个东西不存在」≠「主人看到的东西是什么」**。前一条让我把同一个问题
   答错三次（行号与正文之间那条线），因为我只证明了边框是 0px，从没去枚举接缝上
   到底站着哪些元素。量法见文末「取证脚本」。

---


## 一、2026-09-05 第十七轮批注（3 条，主人在浏览器里点的）

主人原话：「第 16 批批注你改完了，把做完的删掉（你目前打勾的那几项我都认可了）。然后我
给你提了一些新 bug，你按照这个行为逻辑先写进文件里，然后一步一步实践、勾画，最后交给我
审核。」——这套流程已写进 `AGENTS.md`《UI 批注处理闭环》。
**第十六批 9 条经主人认可，按「勾完即删该条」从本文件删除**，结论在
`WORKSTREAM-PLAN.md`《四、历史》一行一条；该批遗留、明确「本轮没做」的两条挪到下面第二节。

### 17.1 ✅ 思考过程的呈现形式：不要方框，也不要一两个字一行（批注 1）

- 主人：「思考模式的呈现形式有问题，不要用这种方框框选的形式。而且，你一个字、一两个字
  一行就不对，文字，你可以采用这样的形式来呈现。**当你运行的是一些命令的时候，你可以采用
  这种形式**」，并给了两张参考图：思考 = 无框、正常成段的文字；命令 = 一条条动作行。
- 现状两条都错：① 展开体用的是 `.chat-trace-body`——一个带边框与底色的 `<pre>` 方框；
  ② 我落地当天修掉的「每个 delta 块用 `\n\n` 拼接」只救了**新数据**，
  库里已存的那几条推理仍是碎的，界面上就呈现成「用户 / 想 / 让我 / 用」一两个字一行。
- 判据：
  1. 思考过程展开后**无边框、无底色方框**（computed `border-top-width: 0px` 且
     `background-color` 为透明），文字 `white-space: normal` 按容器宽度正常折行；
  2. **旧数据也要正常**：呈现层把连续换行归一化成段落分隔，实测展开后不再出现
     「一两个字独占一行」（给出实测行数与首行文本）；
  3. 工具轨迹保持**动作条目**形式（每步一行），与思考过程在视觉上分家——
     这正是主人给的分工：文字归文字，命令归命令；
  4. 断言进 `uiInvariants`：思考过程的容器不得带 border/背景方框，
     `.chat-trace-body` 这个带框样式只留给工具轨迹。
- **已做（2026-09-05 真机展开旧数据量过）**。呈现层新增 `reasoningParagraphs()`
  （放在 `chatTrace.ts`，与 `splitTrace` 同族）：按空行切段后，**若片段长度中位数 ≤ 4
  就判定为被 delta 撕碎、拼回一段**；正常数据原样保留段落。库里已存的碎行**不改写**，
  只在读出时归一——那是主人的数据，我没有权利顺手 UPDATE。
  思考体从 `<pre class="chat-trace-body">` 换成 `<div class="chat-thinking-body">` + `<p>`；
  工具轨迹仍用原来那个等宽方框（主人给的分工：文字归文字、命令归命令）。
  真机（展开**旧的那条碎行**，作品 5）：1 段 137 字、462px 宽处折成 **4 行**、
  `border 0px none`、`background rgba(0,0,0,0)`、`white-space normal`、字体回到正文无衬线
  （不再是 `--mono`）；截图看过，与主人给的「深度思考」参考图同形。
  新增 `chatTrace.test.ts` 5 条（碎行拼回／干净行不动／真多段保留／段内换行去掉但不丢字／
  空值不给空壳）；`uiInvariants` 25 → **26** 块（钉思考体无 border/background/mono、
  钉 ChatPane 里思考折叠不再套 `.chat-trace-body`、反向钉工具轨迹那个方框还在）。
  前端 141 → **146 passed / 19 files**。

### 17.2 ✅ 空态两行文字的中心不在同一条线上（批注 2）

- 主人：「我老是感觉你下面的这行小字的中心，跟上面那个章节编辑的中心不在同一条线上」。
- 判据：`.editor-empty h2` 与 `.editor-empty p` 的 `getBoundingClientRect()` **中心 x 之差
  ≤ 0.5px**（真机量，不靠目测）；并**整类**扫其余空态同一轴。
- **先量，结果和主人的说法不完全一样，但他说的是对的**：那两行彼此的中心**本来就相同**
  （1557 视口下都是 1202.5）；真正偏的是**整个空态块**——它按整栏居中，
  而正文实际住在「整栏 − 56px 缩略栏」那一列里，中心应为 **1174.5**。
  所以他的感觉不是错觉，只是归因不同：不是两行互相偏，是**两行一起偏了 28px**（= 56/2）。
- **整类扫出同一处错的四张脸**（1557 视口实测中心，源码列 = 1174.5 为基准）：
  `.editor-empty` 1203 → **1174.5**；`.toc-list-overlay` 内容 1203 → **1174.5**；
  `.character-doc-overlay` 卡片 1203 → **1174.5**；`.file-cm` 本来就是 1174.5（基准）。
  1200 视口同样偏 28px，不是巧合而是同一个 56px 的一半。
- 改法用 token 不用字面量：`--minimap-w: 56px` 一处定义，`.minimap` 的宽度与三处
  居中内衬都读它——**覆盖层仍盖住装订线**（表格/卡片没有可映射的滚动，盖住是对的），
  只是内容按阅读列居中，于是「源码 ↔ 渲染」来回切不会整页横移。
- 顺带清掉一处**悬空 token**：`--text-3` 全仓从未定义，唯一引用它的是我上一轮删掉的
  `.character-doc-empty`；我新写的一条也误用了它（实测颜色回落到继承才发现）。
  现在 `--text-3` 引用数为 **0**。
- 闸门：`uiInvariants` 26 → **27** 块（钉 token 只定义一次、`.minimap` 与三处内衬都读它、
  并反向钉死 `width: 56px` 这个字面量不许再出现——第二份 56px 就是重新跑偏的方式）。
  真机复验：图标/标题/副标题三者中心都是 **1174.5**。

### 17.3 ✅ 「章节编辑」上面加一个书本图标（批注 3）

- 主人：「可以在这个上面再加一个图标，加那个书本的图标」。
- 与 17.2 是**同一处缺陷的两面**：`§0.6 空态` 规定「lucide 图标 + 一句话」，
  `.file-empty` 有 `FileCode2 size={22}`，而 `.editor-empty` **只有两行字、没有图标**——
  缺一个块，居中基准自然就不一样了。所以两条一起做才有意义。
- 判据：`.editor-empty` 图标在 h2 之上、`BookOpen`（书本＝正文/阅读面，与 16.2 的图标语义
  一致）；**整类**：每个空态都有且只有一个图标，图标 / 标题 / 副标题三者同轴居中；
  图标不进命中区审计的控件清单（`aria-hidden`），不新增可点区域。
- **已做（2026-09-05 真机量过、截图看过）**。`.editor-empty` 加 `BookOpen size={22}`
  （与同栏对的 `.file-empty` 的 `FileCode2 size={22}` 同尺寸）。
  **整类扫出六个面板级空态**：`editor-empty`（缺）／`file-empty`（有，但缺 `aria-hidden`）／
  `library-empty`（缺）／`page-panel-empty` 伏笔墙 `Flag 32`（有）／地图 `MapPin 32`（有）／
  `run-detail-empty` `FileText 24`（有）→ 补齐两处缺的、给装饰性图标统一加
  `aria-hidden="true"`（`Users 24` 用于人物面板）。
  **刻意不加图标的**：`.tree-empty`／`.chat-detail-empty`／`.toc-empty`／`.feedback-empty`
  ——它们是列表/明细里的一行提示，不是撑满一栏的面板级空态，加图标就成了装饰。
- 真机（1557 视口，关掉全部章节标签）：图标 / `h2` / `p` 三者中心 **全部 1174.5**（与 17.2
  修正后的阅读列同轴）；`svg` 的 `aria-hidden="true"`、`getComputedStyle(color)` 继承面板的
  `--text-2`（我一开始误用从未定义的 `--text-3`，实测颜色回落才发现，已删）；
  图标不在任何 `button`/`a` 内，命中区审计控件数不变。
- 闸门：`uiInvariants` 27 → **28** 块——遍历六个面板级空态，逐个要求「块内第一个元素就是
  `<Icon size={n} aria-hidden="true" />`」，缺一个就红；并单独钉死 `.editor-empty` 用的是
  `BookOpen`。前端 147 → **148 passed / 19 files**。

---

## 二、上一批明确「本轮没做」的两条（不是新需求，别当成已修）

### 16.10 本轮清理时新量出来的既存缺陷（只记录，不混进上面 9 条）

- [ ] **对话里代码块的「复制这段代码」钮有 6px 被 `<pre>` 盖住**：命中区审计
      `28x28` 的框只走到 `28x22`。逐行 `elementFromPoint` 取证：把该钮滚到视口中部时
      25..52 行全部命中、只有底边 1 行被 `PRE` 压住；而审计不滚动、按当时版面量到 22。
      **已证明与本轮删样式无关**：拿 HEAD 的 `styles.css` 重跑同一审计，数字一模一样。
      判据：`.chat-code-bar` 与 `<pre>` 的层叠关系理顺后，该钮命中 = 28×28。
  （同族第三处已在 16.1 那轮修掉：审计把被窗口**上沿**裁掉的控件误报成 unreachable，
  现在四条边都先归 `edge`。）
- [x] 顺带修一条度量教训：`hit-area-audit.mjs` 原来 `STEP=2`，遇到落在半像素上的框
      （`top=425.75`）每边白丢 1px，把 28×28 报成 27×21——**假警报**。已改 `STEP=1`。
      本轮门禁的期望值因此是 **1 small（就是上面这条）**，不是 0；谁把它调回 0 就是在藏东西。

### 16.11 做 16.8 时量出来的另一处「按一下没反应」（责任提交是我自己的 `6b6dbd3`）

- [ ] **`?file=chapters/NNNN/draft.md` 深链进来时，钮的朝向与画面相反**：
      真机实测——落地瞬间画面是 CodeMirror 源码，但 `views[path]` 是 `false`，
      于是钮说「切到源码视图」（把它已经看着的东西再承诺一遍），
      **第一次点击只翻标志、不换画面**，第二次才跳去正文页。
- 根因：`views[draftPath]` 有**两个写者**且互相覆盖，两个都出自
  `6b6dbd3`（第十五批 1.3/1.4/3.2/3.4，我那一轮）：`files.ts` 的 `open()` 对 draft 路径
  stamp `true`，`workbench.ts` 的 `hydrateChapterDraft()` 无条件 stamp `false`。
  深链与「点章节行」两条路径都同时触发两者，谁后跑谁赢——实测 hydrate 后跑。
- **这条不是给 hydrate 加个 `if` 就完事**：标志的真正含义是「哪一栏在台上」，
  而「哪一栏在台上」由 `WorkbenchPage.rightView` 决定（React 态，不在 store 里）。
  正确改法是**让决定栏位的那段代码同时写这个标志**：
  `open()`／reveal 效应 → `true`；`selectChapter` 且没有文件 reveal → `false`；
  hydrate 不再碰它。改完要把 16.2 的映射与第十五批 1.4 的往返一起复验。
- 判据：深链 `?file=chapters/0001/draft.md` 落地即 `aria-pressed=true` 且文案「切到正文页」；
  一次点击就到正文页；从树里点章节行（同时开文件）时标志与画面一致；
  从树里只选章（不开文件）时标志为 `false`。

---

---

## 三、第九批改完之后新开的（1 条，2026-09-04）

> 另两条由第十批的颜色改动一并解决：活动条改到了与侧栏同档，
> 弹层用的 `--surface` 现在比列底高一级，不再往下沉。

- [ ] **未配置提示的文案在教主人做一件错事**：`.chat-notice` 写「在 backend/.env 填入密钥后
      重启后端」，而 D-16 之后配置的真源是 `app_config` 表、入口在 `/settings`。
      一句话改口的活，第九批没顺手做（不在批注范围内）。

---

## 四、前几轮欠着未做的（按主人点名次数排序）

[x] ~~章节编辑器标签条也要有同一个切换钮~~（已做，见 §1.4 / §3.4：钮在
      `.editor-tabs-actions`，状态就是 `useFiles.views[path]`，投影全文由 `ensure()` 取）。
- [x] ~~人物卡片的长字段目前是只读预览，没有「渲染 ↔ 原文件」这一对~~
      （已做，见 §3.1：整份 `settings/characters/N.md` 的渲染视图就是人物卡片，
      四个长字段都在卡上，钮与文件条共用同一个 `views[path]`）。
- [ ] **设置页改成「列表 + 入口」的可拓展结构**（主人点了 3 次）。
      现在整页都是「模型接入」，主人明确：以后要加更多设置项，必须预留结构。
- [ ] **色系切换**（含**代码配色**与**字体切换**）。主人说现在这套字「复古、像上个世纪网页文字」；
      另外早前就要求**新增一套蓝色系**。
- [ ] **书卡右键菜单**：更换封面 / 删除 / 其他改动收进右键（主人：「按钮能用图标就用图标」）。
- [ ] **封面色改成调色盘**（或至少加一个调色盘入口）。
- [x] ~~拖动分栏边界到极限应能关闭该栏~~（已做，见上面 §2.2，本条从第四节划掉）。
- [ ] **四条 bar 收窄并统一**：`.file-tabs` / `.editor-tabs` 同宽，`.editor-toolbar` 略窄，
      `.topbar` 与首页顶栏同高。
- [ ] **对比度剩两处（品牌色决定，需主人拍板）**：`--accent` 压自己的淡色底 4.29；
      深色主题白字压 `--accent` 3.31。
- [ ] **伏笔墙 / 地图面板接上真实数据源**：面板内**零请求**，是未接线，不是没数据（别再重复排查）。

---

## 五、被后端语义卡住的（不要在前端臆造第二条写通路）

- [ ] 树右键「重命名 / 删除」：全仓除 `chat.py` **无任何 delete 路由**。
      与 **Q-07**（反馈记录进不进文件层）、**Q-09**（人物删除必然 405）、「删除作品」是**同一条决策**，一起定。
- [ ] 书架上误建的 `dbg`（novel id=6）删不掉，同因。

---

## 六、取证脚本（每轮 UI 改动后跑一遍，别靠目测）

放在 `.scratch/`，已验证可用的入口：

- `cdp.mjs` → `connect(url)` 返回 `{ evaluate, click, clickPoint, send, shot, close }`；
  无 Chrome，用裸 Edge + CDP（端口 9333）。
- **写 CDP 脚本一律 `-Encoding utf8`**。用 `ascii` 会把脚本里的中文静默变成 `?`，
  于是 `find(n => n.getAttribute("aria-label") === "展开调用记录")` 永远匹配不上，
  报出来的「控件不存在」全是脚本 bug——本会话被它误导三次。
- **命中区审计（已写）**：`node .scratch/hit-area-audit.mjs [url] [open-file]`。
  它**不信 CSS 盒子**——上一轮就是盒子骗的我（`.tree-prefix` 写着 28px，实际被 overflow
  裁掉）——而是用 `elementFromPoint` 从控件中心向四向走，量「真点得到」的范围，
  伪元素覆盖层和负 margin 都算进去。三段输出：`small`（命中 < 24px）、
  `clipped`（命中框任何一部分落在最近滚动/裁切容器之外）、
  `unreachable`（中心被别的东西盖住）。改完任何一处控件尺寸都跑一遍。
- **对比度审计**：逐元素算合成背景与前景的 WCAG 比值（浅色主题曾一次抓出 13 处不过 AA，
  其中十处共用同一个 `--text-2`）。
- **量「主人看到的线」的正确方法**：给定一条视觉接缝，枚举 `elementFromPoint` 沿途
  以及两侧容器**所有子元素**的 `left/right/backgroundColor`，不要只查你怀疑的那个元素有没有 border。

---

## 七、门禁（每次推送前必须全绿）

```
cd E:\novel-generator\backend;  .venv\Scripts\python.exe -m pytest -q
cd E:\novel-generator\frontend; npm run test -- --run
cd E:\novel-generator\frontend; npx tsc -b --pretty false
cd E:\novel-generator\frontend; npm run build
git -c http.proxy=http://127.0.0.1:7890 push origin main
```

测试要**跟着设计改断言**，不许删。`WorkbenchLayout.test.tsx` 里那条把「按两次方向键」
写成 `300 + 32` 的算式而不是字面量——上次它绑死 312，因为默认值变了而以错误的原因失败。
