# UI 待清账（勾完即删该条）

职责边界：本文件**只记未完成的界面条目**。理由在 `DECISIONS.md`，进度史在
`WORKSTREAM-PLAN.md`，视觉规格在 `UI-DESIGN.md`。做完一条就勾掉并在
`WORKSTREAM-PLAN.md` 写一行结论，不要在这里堆历史。

**开工前必读的三条纪律**（每一条都是踩出来的）：

0. **主人新提的任何东西，先写进本文件，再动手；一条都不留在对话里。**
   2026-09-05 主人原话：「你先把这几轮没弄的需求都给我记到 MD 文件里面去，
   **不要再在这里靠记忆了**。」同一轮里他还说过「我每次跟你提了之后，你老是经常容易忘记」。
   记忆不是存储，对话必被截断。落不进文件的请求等于没提。
1. **效果类改动必须真机截图、自己看过**才算完成。只看 diff 不算。
2. **「我怀疑的那个东西不存在」≠「主人看到的东西是什么」**。前一条让我把同一个问题
   答错三次（行号与正文之间那条线），因为我只证明了边框是 0px，从没去枚举接缝上
   到底站着哪些元素。量法见文末「取证脚本」。

---


## 一、2026-09-06 第十九轮批注（3 条，全部已做，等主人审批）

主人：「提了一些意见，已经做完的审批通过，可以把新的意见加进去，继续按顺序做。」
19.1（附件 chip）与 19.2（多供应商 + 任务路由）**已通过审批**，连同第十八批（18.1 思考过程
字体／18.2 工具轨迹条目／设置页列表+入口）与第十七批，一律按「勾完即删该条」从本文件删除，
结论在 `WORKSTREAM-PLAN.md`《四、历史》。

### 19.3 外观页做完善：VSCode 式切换 + 色系 + 代码配色 + 字体（批注 3）

- 主人：「外观这里你没有做到完善，你可以采用像 VSCode 这样的切换设计。
  像你的色系切换，这些也都还没来得及做。外观这里应该是可以调这些偏好的」，
  参考图是 VSCode：主题三卡（系统／浅色／深色，各带真实预览缩略）+ 下方 diff 预览 +
  强调色／背景等逐项色值。
- 现状：「外观」面板只有两个按钮（浅色／深色），色系切换、代码配色、字体切换是
  第四节挂着的老账（主人点过 3 次的那堆里就有）。
- 判据：
  1. 三张主题卡：**系统 / 浅色 / 深色**，「系统」是跟随 `prefers-color-scheme`（新行为），
     每卡带一个用当前 token 画的迷你预览，而不是纯色块；
  2. 色系：现有朱砂橙保留为默认，**新增一套蓝色系**（老账明确要求），切换后
     `--accent` 家族整体换、对比度仍过 AA（用对比度审计复量，别只看着像）；
  3. 代码配色与字体切换：至少各一个可选值，且 CodeMirror 与正文真的跟着变（不是只存值）；
  4. 偏好存哪要有个说法并写进 DECISIONS：外观是**这台机器的偏好**，与
     D-16（服务端配置）分清楚，别把主题塞进 `app_config` 让另一台机器被动跟随；
  5. 所有选择刷新后仍在；键盘可达（卡片是 `role="radio"`，同 §0.6 弹层与分级控件口径）。

- **已做（2026-09-06 真机点过、截图看过）**。
  · 四组开关都落在 html 的 `data-theme` / `data-accent` / `data-code` / `data-prose` 上，
    只由 `store/appearance.ts` 写；`workbench` 里那份 `theme`／`toggleTheme` 删除——
    两个写者覆盖同一个属性，正是 3.3 与 16.11 的老病。存 `localStorage.appearance` 一个键，
    旧的 `localStorage.theme` 读回来搬进新键，主人不必重选。理由与被否写法记在 **D-21**。
  · 「系统」是新行为：写入那一刻解析成 light/dark，CSS 里没有第三种值；机器自己切换时由
    `matchMedia` 的 change 事件重解析。真机两条都验：不刷新浅→深（`stored:null`）；
    显式选浅色的时候机器转暗**不跟**（`mq:true` 而 `attrs:light`）。
  · 预览卡不抄色值：浅色那套 token 挂在 `:root, [data-theme="light"]` 上，卡片自带
    `data-theme`/`data-accent`/`data-code`/`data-prose` 就能画「另一个主题长什么样」。
    实测卡片 208×113、四组共 9 张、`role="radio"` + `aria-checked`，Tab 可达，
    聚焦 `outline:none` 且无 `box-shadow`（§0.9「聚焦不画一圈」仍成立）。
  · 色系：朱砂留作默认，新增蓝。CodeMirror **真的**跟着换——`arcs.md` 源码视图里
    标题文字 rgb(194,73,47) → 蓝 rgb(53,104,160) → 石墨 rgb(36,35,34)。
  · 正文字体：`.editor-body textarea` 计算值 `"Noto Serif SC", "Source Han Serif SC", serif`
    → `"Noto Sans SC", "Segoe UI", …`，字号仍 17px；缩略栏 canvas 改用
    `tokenValue("--prose")` 回读，`EditorPane` 里那份字体栈字面量删掉。
  · 对比度复量（工作台 1440×1000 逐元素）：浅朱砂 **0**／浅蓝 **0**／深蓝 **0**／
    深蓝+石墨+黑体 **0**；深朱砂 **1** —— 禁用态主按钮 `#9a8883` 压 `#3a2b27` = **4.00**（需 4.5），
    **改前就有**；蓝那一套我换成 5.07，朱砂是否一起抬上去属品牌色决定 → 已登记进第二节。
  · 测试：新增 `store/appearance.test.ts` 5 条、`PreferencesPage` 1 条；
    `uiInvariants` 33 → **35** 块（钉住复合选择器、预览不许抄色值、正文字体一个出处、
    `data-theme` 一个写者）。前端 163 → **171 passed / 20 files**；tsc clean；build clean；
    命中区 `/settings` 外观 0 small / 0 clipped / 0 unreachable，`/novels/5` 同
    （edge 1、srOnly 1 属既有）。

---

## 二、前几轮欠着未做的（按主人点名次数排序）

[x] ~~章节编辑器标签条也要有同一个切换钮~~（已做，见 §1.4 / §3.4：钮在
      `.editor-tabs-actions`，状态就是 `useFiles.views[path]`，投影全文由 `ensure()` 取）。
- [x] ~~人物卡片的长字段目前是只读预览，没有「渲染 ↔ 原文件」这一对~~
      （已做，见 §3.1：整份 `settings/characters/N.md` 的渲染视图就是人物卡片，
      四个长字段都在卡上，钮与文件条共用同一个 `views[path]`）。
- [x] **设置页改成「列表 + 入口」的可拓展结构**（主人点了 3 次）。
      **已做（2026-09-05 真机点过、截图看过）**。`PreferencesPage` 现在是一张
      `groups: {key, label, body}[]` 表 + 左列 `role="tablist"` 列表 + 右侧唯一面板
      （`role="tabpanel"`）；**以后加「色系 / 代码配色 / 字体」就是往表里添一项，不动布局**。
      顺手修掉一处自己刚造出来的重复：面板里原本又放了一个 `<h2>模型接入</h2>`，
      左列那条已经说过一遍了——正是 §0.7 条六。删掉 h2，改用
      `aria-labelledby="prefs-tab-…"` 指回列表项，读屏仍听得到分组名。
      实测（1323 宽）：nav 196px @x162、panel 776px @x386、两行各 32px 高；
      默认「模型接入」显示 7 个输入框、无主题按钮；点「外观」后
      `aria-selected` 互换、面板换成 2 个 `role="radio"`、输入框归零；
      「外观」二字在屏上**只出现一次**。新增 1 条测试钉住这些。
      窄屏（≤760px）列表转成横排、取消 sticky。
- [x] ~~色系切换（含代码配色与字体切换）~~ → 已并入 **19.3**，不在这里重复挂一条。
- [ ] **书卡右键菜单**（前几轮点名项，2026-09-06 开工前补判据）
      主人原话：「书卡右键菜单：更换封面 / 删除 / 其他改动收进右键」「按钮能用图标就用图标」。
      现状取证：`BookshelfPage.tsx:184-195` 卡面那枚「更换封面」是**图标 + 文字**的胶囊
      （`.cover-change-btn`，`opacity:0` → `.book-card:hover` 才出现，去常驻已经做对了）；
      全仓 `onContextMenu` 只挂在树上（`TreePane.tsx:308/347/480`），**书卡右键今天什么都不做**。
      树那一侧已有 `.tree-menu` 一整套，含「未开放」的 disabled 条目，是本项目认可过的口径。
      判据：
      1. 右键书卡出菜单：打开作品 / 更换封面… / 删除作品。前两条真的做事；第三条
         **照树的口径 disabled + title 写原因**（删除语义未定，见第三节）。
         **不为凑菜单开第二条写通路、不做假按钮**；
      2. 菜单是页面级 fixed 浮层（复用 `.tree-menu`，不新写一套）；点外面与 Esc 都关，
         关闭后焦点回到那张卡；键盘 `Shift+F10` / 菜单键同样能开；
      3. 卡面那枚胶囊改成**纯图标**（`aria-label` + `title` 说清是哪本书），仍是悬停/聚焦才
         出现；尺寸按 §0.4「独立图标 32×32」——实测书卡有约 26° 的 3D 斜躺变换，
         一枚 28px 的钮在屏幕上命中宽只剩 24px（28 × cos26），32px 压完是 28.7px；
      4. 点菜单项不许顺带触发卡片的导航：实测点「更换封面」停在书架并打开封面弹窗；
      5. 测试：右键出菜单三条齐全／删除项 disabled 且有原因／Esc 关／点更换封面开弹窗
         且不跳工作台／卡面按钮不含文字；
      6. 真机：右键菜单截图自己看过；命中区审计 0 small / 0 clipped / 0 unreachable。

- **已做（2026-09-06 真机右键过、截图看过）**。
  · 右键书卡出菜单（真鼠标右键，CDP `Input.dispatchMouseEvent`）：`打开作品 Enter`／
    `更换封面… 悬停图标`／分隔线／`删除作品 未开放`。菜单 `aria-label="《九霄观星录》的操作"`，
    三条命中区各 **198×32**；删除项 `disabled` + `title` 写明「删除语义未定」，
    **没有**为凑菜单开第二条写通路（第三节）。
  · 两扇门：`onContextMenu` 与键盘 `Shift+F10`／`ContextMenu`；点外面与 Esc 都关，
    Esc 关完焦点回到那张卡（实测 `document.activeElement === card`）。
  · 卡面胶囊改成**纯图标**：`textContent` 为空、`aria-label`+`title` 说清是哪本书；
    尺寸定 **32×32** 而不是 28——书卡整体有约 26° 的 3D 斜躺变换，实测 28px 的钮
    在屏幕上命中宽只剩 **24px**（28 × cos26），正好压在审计地板线上；32px 压完 **28×32** ✓。
  · **截图抓到一个既存缺陷**（T-19）：全局 `button.primary` 是 (0,1,1)，压过
    `.tree-menu-item` 的 (0,1,0)，于是菜单第一项被刷成**实心主色条**、条上的字也是主色，
    整行看不见。这个病在树菜单里已经存在很久，jsdom 测试永远看不见（它不断样式表）。
    修在共用类上（`.tree-menu-item.primary { background: none }`），两张菜单一起好。
  · 测试：`BookshelfPage` +3（右键三条齐全且删除项 disabled 带原因／Esc 关并还焦点／
    键盘 Shift+F10 能开／点更换封面开弹窗且不跳工作台）；`uiInvariants` 35 → **36** 块。
    前端 171 → **175 passed / 20 files**；tsc clean；build clean；
    命中区 `/` 与 `/novels/5` 均 0 small / 0 clipped / 0 unreachable。
- [ ] **封面色改成调色盘**（前几轮点名项，2026-09-06 开工前补判据）
      现状取证：封面弹窗里已有八枚预设 + 一枚「默认」（`COVER_COLORS`，`role="radiogroup"`），
      但**只能从八个里挑**；而且 `coverColor` 今天**不驱动弹窗预览**
      （`.cover-preview span` 用的是全局 `--accent`，选完要保存回书架才看得见）。
      后端 `_check_color()` 只校验 `#rrggbb`，所以自由取色不需要新端点、不动 schema。
      判据：
      1. 色板末尾多一枚**真·调色盘入口**：`<input type="color">`（系统取色器），
         26×26 圆形命中 ≥24、`aria-label="自定义封面颜色"`；**不自己画一个假色轮**；
      2. 它**不进** `role="radiogroup"`（radiogroup 的子项必须全是 radio），做同一行的兄弟节点；
      3. 选完**当场**看得见：弹窗预览的书名字色随所选变，未选色时回落到主色；
         写回仍走 `PUT /api/novels/{id}` 一条口（D-01）；
      4. 当前色不在八枚预设里时，选中态（描边环）落在这枚自定义上；回到预设之一时相反；
         「默认」仍能把值清回空——自定义不许吃掉「不设色」这个选项；
      5. 测试：选一个非预设色 → 保存的 PUT body 是那个 hex、预览带上 `--book-accent`、
         自定义钮的选中态随值切换、清回默认仍是空值；
      6. 真机：色板截图自己看过；命中区审计 0 small / 0 clipped / 0 unreachable
         （`input` 在审计的选择器里，这枚必须过 24px）。

- **已做（2026-09-06 真机取色、保存、还原，截图看过）**。
  · 色板末尾多一枚 `input[type=color]`（系统取色器），`aria-label="自定义封面颜色"`；
    实测命中区 **26×29**（预设同尺寸 26，审计地板线 24）✓，`closest('[role="radiogroup"]')`
    = **false** ✓（radiogroup 的子项必须全是 radio，它是取色器不是 radio），预设仍是 9 枚 radio。
  · **形状故意与预设不同**：预设是圆、它是方（`border-radius: 7px`）。第一版我让它跟预设一样圆，
    截图里它和「青碧」是两枚一模一样的绿圆，只有悬停 tooltip 能分——而 tooltip 只有鼠标知道。
  · 挑完当场看得见：预览的书名字色绑到 `--book-accent`，实测挑 `#7a3fd1` 后
    计算色 = `rgb(122,63,209)`、内联样式 `--book-accent: #7a3fd1`、`.on` 描边环归这枚、
    保存钮从 disabled 变 enabled；真点保存后 `GET /api/novels` 回读 `#7a3fd1` ✓
    （写回仍只有 `PUT /api/novels/{id}` 这一条口，D-01；后端只校验 `#rrggbb`，没动 schema）。
    **测完把六本书的 `cover_color` 逐本还原成测试前的值**（1:#2f6b57 2:"" 3:#5a4668
    4:#2f4a63 5:"" 6:""），并回读核实；另一条走「取消」的路径也验过：不落库。
  · 一处 React 事实（T-20）：想让「未选色」回到 CSS 回落，写法是
    `style={coverColor ? {...} : undefined}`——**React 不会把内联自定义属性清回去**，
    测试里量到节点上留的还是上一个 hex。改成永远给一个具体值（空值时给主色）。
  · 测试：`BookshelfPage` +1（取色器在组外／挑完预览与选中态都变／保存落库 hex／
    重开后认得出非预设色／回到预设与「默认」都各自正确）；`uiInvariants` 36 → **37** 块。
    前端 175 → **177 passed / 20 files**；tsc clean；build clean；命中区 0/0/0。
- [x] ~~拖动分栏边界到极限应能关闭该栏~~（已做，见上面 §2.2，本条从第四节划掉）。

- [ ] **对比度剩三处（品牌色决定，需主人拍板）**：`--accent` 压自己的淡色底 4.29；
      深色主题白字压 `--accent` 3.31；深色**禁用态主按钮** 4.00
      （`--control-disabled-accent-fg #9a8883` 压 `--control-disabled-accent-bg #3a2b27`，
      第十九批 19.3 复量时抓到，改前就有。蓝色系那一对已换成 5.07，朱砂要不要一起抬，一起拍）。

- [ ] **`UI-DESIGN §0.1` 那张表有三个值和代码不一致**（19.3 顺手发现，本轮不改）：
      表里 dark `surface #1C1C1E`、`surface-alt #26262A`、light `text-2 #73716C`，
      代码是 `#1f2023`、`#191a1b`、`#6d6b66`（后两处正是第十轮批注 1 与 `--text-2` 那一次修定的）。
      这是文档漂移不是界面缺陷，但它是「文档说的和跑着的不是一回事」，下次动 §0.1 一并校。

---

- [ ] **伏笔墙 / 地图面板接上真实数据源**：面板内**零请求**，是未接线，不是没数据（别再重复排查）。
      **2026-09-05 主人明确排到最后：「伏笔墙跟地图的数据接线往后靠，先把其他的做了」**

---

## 三、被后端语义卡住的（不要在前端臆造第二条写通路）

- [ ] 树右键「重命名 / 删除」：全仓除 `chat.py` **无任何 delete 路由**。
      与 **Q-07**（反馈记录进不进文件层）、**Q-09**（人物删除必然 405）、「删除作品」是**同一条决策**，一起定。
- [ ] 书架上误建的 `dbg`（novel id=6）删不掉，同因。

---

## 四、取证脚本（每轮 UI 改动后跑一遍，别靠目测）

放在 `.scratch/`，已验证可用的入口：

- `cdp.mjs` → `connect(url)` 返回 `{ evaluate, click, clickPoint, send, shot, close }`；
  无 Chrome，用裸 Edge + CDP（端口 9333）。
- **写 CDP 脚本一律 `-Encoding utf8`**。用 `ascii` 会把脚本里的中文静默变成 `?`，
  于是 `find(n => n.getAttribute("aria-label") === "展开调用记录")` 永远匹配不上，
  报出来的「控件不存在」全是脚本 bug——本会话被它误导三次。
- **命中区审计（已写）**：`node .scratch/hit-area-audit.mjs [url] [open-file]`。
  它**不信 CSS 盒子**——上一轮就是盒子骗的我（`.tree-prefix` 写着 28px，实际被 overflow
  裁掉）——而是用 `elementFromPoint` 从控件中心向四向走，量「真点得到」的范围，
  伪元素覆盖层和负 margin 都算进去。三段输出：`small`（命中 < 24px）、
  `clipped`（命中框任何一部分落在最近滚动/裁切容器之外）、
  `unreachable`（中心被别的东西盖住）。改完任何一处控件尺寸都跑一遍。
- **对比度审计**：逐元素算合成背景与前景的 WCAG 比值（浅色主题曾一次抓出 13 处不过 AA，
  其中十处共用同一个 `--text-2`）。
- **量「主人看到的线」的正确方法**：给定一条视觉接缝，枚举 `elementFromPoint` 沿途
  以及两侧容器**所有子元素**的 `left/right/backgroundColor`，不要只查你怀疑的那个元素有没有 border。

---

## 五、门禁（每次推送前必须全绿）

```
cd E:\novel-generator\backend;  .venv\Scripts\python.exe -m pytest -q
cd E:\novel-generator\frontend; npm run test -- --run
cd E:\novel-generator\frontend; npx tsc -b --pretty false
cd E:\novel-generator\frontend; npm run build
git -c http.proxy=http://127.0.0.1:7890 push origin main
```

测试要**跟着设计改断言**，不许删。`WorkbenchLayout.test.tsx` 里那条把「按两次方向键」
写成 `300 + 32` 的算式而不是字面量——上次它绑死 312，因为默认值变了而以错误的原因失败。
