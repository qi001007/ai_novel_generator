# UI 待清账（勾完即删该条）

职责边界：本文件**只记未完成的界面条目**。理由在 `DECISIONS.md`，进度史在
`WORKSTREAM-PLAN.md`，视觉规格在 `UI-DESIGN.md`。做完一条就把结论写成
`WORKSTREAM-PLAN.md` 的「已经能用的 / 还差什么」里改一句，然后**把整条从本文件删掉** -
不要在这里堆历史。过程细节不用抄：每条批注的提交信息里都带着实测数字，
`git log --oneline` 与 `git show <提交>:docs/UI-BACKLOG.md` 随时能翻回来。
处理闭环（先写文件→按严重度一条一条做→真机截图→同批改断言→单独提交）在
`AGENTS.md`《UI 批注处理闭环》。

**开工前必读的三条纪律**（每一条都是踩出来的）：

0. **主人新提的任何东西，先写进本文件，再动手；一条都不留在对话里。**
   2026-09-05 主人原话：「你先把这几轮没弄的需求都给我记到 MD 文件里面去，
   **不要再在这里靠记忆了**。」同一轮里他还说过「我每次跟你提了之后，你老是经常容易忘记」。
   记忆不是存储，对话必被截断。落不进文件的请求等于没提。
1. **效果类改动必须真机截图、自己看过**才算完成。只看 diff 不算。
2. **「我怀疑的那个东西不存在」≠「主人看到的东西是什么」**。前一条让我把同一个问题
   答错三次（行号与正文之间那条线），因为我只证明了边框是 0px，从没去枚举接缝上
   到底站着哪些元素。量法见文末「取证脚本」。

---

## 〇、2026-09-06 第二十五轮批注（2 条界面 + 2 条导出 + 1 个大功能 + 1 条数据 + 1 份计划）

主人这一批**把第二十四批整体通过**了，所以那 7 条已从本文件删掉（结论在
`WORKSTREAM-PLAN §一/§二/§三`、`DECISIONS D-26 / D-27`、`UI-DESIGN §0.9` 与逐条提交信息里）。
他还给了一句工作方式：**「这种已经定过的事不要一直回来问我」**（`[TOOL_CALL]` 那条脏数据），
已记进 `DECISIONS §5`。

做单顺序按严重度：**25.1 是死路 bug，先做**；然后两条小的字面（25.2）与导出覆盖面（25.3）；
再往下是成对的两条（25.4 默认目录 / 25.5 备份与撤销，他要求共用同一个设置项，不能做成两个）；
25.6 是他授权的数据删除；25.7 只出计划不动手。

### 25.1 左栏右键「折叠」之后**再也打不开**（批注 2）

- 主人：「这里我右键的选项里选择折叠之后，就再也打不开了」
- 真机复现（CDP，量出来的，不是推测）：右键 `A 全本蓝图` 行 → 菜单里点「折叠 / 展开」→
  `nav.tree` 只剩 `tree-page-head`（高 32px），`.tree-root-plan` 的 rect 是 **0×0**、
  `display: none`，`planRows` 从 **33 → 0**。**回头路被藏起来了。**
- 根因是**我自己造的**：为消掉「规划」这个词在同一侧出现三次加的那条
  `.tree > .tree-root { display: none }`（第二十批 §0.7 条六），把折叠后**唯一**能点开的那一行也藏了；
  当时注释还写着「不删掉，因为它挂着折叠状态」——状态留着了，能点它的那张脸没了。
- 判据：
  1. **折叠态必须留一条看得见的回头路**：`.tree > .tree-root[aria-expanded="false"]` 恢复 `display: flex`
     （展开态仍隐藏，§0.7 条六不回退——那时屏上已有页面名与内容，不需要第三行）；
  2. 页头那枚折叠钮的**标签必须与状态一致**：本页只要有任何东西处于折叠，就写「展开全部」、
     点一下展开全部；只有全展开时才写「折叠全部」。现在的判据是 plan+library+每一章+每一组
     **全部**折叠才改口，部分折叠时点它只会折得更多——这正是他说的「打不开」的另一半；
  3. 真机走完整三步并截图两张（折叠态那一行看得见 / 点回来之后），**自己看过**；
  4. 断言：`uiInvariants` 钉「折叠态的 root 必须有 display 覆盖」；测试钉「菜单折叠后 root 仍在且能展开」；
  5. 命中区审计 0 small / 0 clipped / 0 unreachable。

- **已做（2026-09-06 真机三步走通，折叠态截图看过）**。
  · 折叠后那一行回来了：真机量得 `.tree-root-plan` 从 **0×0 / display:none** 变成
    **244×32**，鼠标真点它 → 行数 **0 → 33**。页头那枚钮的标签也跟着状态改口：
    部分折叠时现在是**「展开全部」**（以前写「折叠全部」，点下去只会折得更多）。
  · 代价说清楚：折叠态下这一侧会出现两次「规划」（页头一次、这一行一次）。
    展开态照旧只出现一次（§0.7 条六不回退）。我选了「宁可重复一行，不要一条死路」；
    主人若觉得这行别扭，改文案（如「展开 规划」）是一行的事。
  · 旧的 `preCollapse`（第二次点击恢复上一次展开）随标签口径一起删了 - 留着就是半死状态。
  · 断言 + 测试：`uiInvariants` 钉「折叠态必须有 display 覆盖」与「标签由 anyCollapsed 决定」，
    并反向钉 `preCollapse` 不许回来；`WorkbenchLayout` 新增一条走完整流程
    （右键 → 折叠 → 那一行仍在 → 点它 → 行数回来）。前端 199 → **201**。
  · 命中区审计 0 small / 0 clipped / 0 unreachable。

### 25.2 对话详情那行 token 要用「思考过程」那种字（批注 1）

- 主人：「这个用思考过程中用的那种字体」（指 `输入 3033 / 输出 386 tokens · MiniMax-M2.5`）
- 判据：`.chat-detail-line` 与它下面的 `.chat-detail-empty` 换成与思考过程同一张脸
  （`font-style: italic` + `--text-2` + 12px + 行高 1.7），**不再用 `--text-1`**；
  真机截图与思考过程并排比过；`uiInvariants` 钉住这两处不许回到 `--text-1`（第十七批那条同类）。

- **已做（2026-09-06 真机量过计算样式并截图看过）**。
  · 改前实测：那行 token 走的是**正文那张脸**（`13px / normal / rgb(236,235,233)`）——
    因为 `.chat-detail-line` 是单类 (0,1,0)，压不过 `.chat-card p` 的 (0,1,1)，
    源码里那 11px **从来没生效过**。改后实测：`12px / italic / rgb(157,155,150) / 1.7`，
    与思考过程同一张脸；正文仍是 13px / normal / --text-1（对照量的是同一屏的两个元素）。
  · 顺带把同一块里的「本轮没有检索到资料」那行统一成同一张脸（两处以前一个 11px 一个 11px，
    但一个用 --text-1 一个用 --text-2，且都被 `.chat-card p` 压掉）。
  · 断言：`uiInvariants` 钉住两处必须带 `.chat-card` 前缀且含 italic / --text-2 / 12px，
    并反向钉「去掉前缀的单类选择器不许回来」——那是第十七批同一条坑的第二次命中。

### 25.3 导出不止正文：蓝图 / 目录 / 剧情弧 / 每章 brief 都要能导（批注 4）

- 主人：「我说的导出功能，希望像全版蓝图、目录、剧情弧，包括每章的 brief 这些都要有导出功能」
- 判据：
  1. 文件树**每一片叶子**（`blueprint.md`／`toc.md`／`arcs.md`／`chapters/*/brief.md`／
     `settings/*`／`draft.md`）右键都有「导出为 Markdown」，导的就是屏上源码面那份文本；
  2. 章节行与 `draft.md` 的两行**导出本章 txt/md** 保留（第二十四批那条不回退）；
  3. **不新增后端端点**：投影 `GET /api/novels/{id}/files/{path}` 已能取全文，前端拿它落盘；
  4. 实测五类文件各导一次，字节数与源码面显示的字符数逐一对得上（不许只测一类就宣布通了）；
  5. 空文档（0 字）也要能导，但界面要说「这份是空的」（简报没写过就是空的，这是常态）。

- **已做（2026-09-06 真机五类各导一次，全部读回核对字符数）**。
  · 文件树**每一片叶子**的右键多了一行「导出为 Markdown」，导的就是屏上源码面那份文本。
    真机下载后读回，字符数与投影 `GET /files/{path}` 返回的 `text.length` **逐一对齐**：
    `blueprint.md` 226 → `全书蓝图.md`；`toc.md` 455 → `目录.md`；`arcs.md` 165 → `剧情弧.md`；
    `chapters/0001/brief.md` 246 → `第 1 章简报.md`；`settings/characters/6.md` 210 → `沈砚舟 档案.md`。
  · 文件名用**这份文档自己的 label**（后端给的），不是 `chapters_0001_brief.md` 这种拼路径；
    非法文件名字符换成 `_`。
  · **`draft.md` 故意不给这一行**（实测它的菜单 6 项、其余叶子 5 项）：它已经有
    「导出本章…」与「导出本章 Markdown」两行，再来一行名字不同、意思几乎一样的动作，
    只会让人猜哪条才是他要的。各有测试钉住这两面。
  · 空文档不静默导出：拿回的内容剥完空白就报错「这份文档还没有内容，导出来是空的」，
    不落 0 字节文件（与第二十四批同一条口径）。
  · **不新增后端端点**：走的就是既有的投影读取。
  · 顺手记下**没改**的：树里叫「全本蓝图」，投影 label 与注入清单叫「全书蓝图」
    （`TreePane.tsx:71` vs `documents.py:509/557`）——同一份资料两个名字，
    与刚修的「分卷 / 剧情弧」是同一条病（§0.7 条三）。登记在 §一，本轮不动手。

### 25.4 设置里新开一栏：默认导出目录（批注 3）

- 主人：「可以把『导出的路径』加进设置里面，新开一个栏目……到时候将全书或某一章导入到哪个路径里」
- 判据：
  1. 设置页多一个栏目（与「模型接入」「外观」同级），名字要同时管导出与恢复——**一个目录设置项，两处共用**，
     不许出现第二个（他原话：尽量保证简洁性与通用性）；
  2. 设了这个目录 → 导出直接写到那儿并在界面回报**完整文件路径**；没设 → 走浏览器下载并明写「未设置导出目录」；
  3. 写盘是**磁盘写**，不是数据库写：四层规划那条唯一写入口（D-01）不受影响，理由记进 `DECISIONS`；
  4. 路径必须是绝对路径且由后端只写它自己命名的文件（`书名_全书.txt` 这种），越界与非法字符要拒；
  5. 后端 ≥3 条测试（写到指定目录 / 未配置时不写盘 / 非法路径拒），前端 ≥2 条（这一栏存在、值能存能读回）。

- **已做（2026-09-06 真机验过两条落点，三张截图看过；测完把目录恢复成主人原来的空值）**。
  · 设置页多一栏**「导出与恢复」**（左列第三项），里面**只有一个目录设置项**——
    导出与「从快照里取文件」共用它，没做成两个（他明确要求）。
  · 一条导出、两条落点：设了目录 → 后端写盘并回报完整路径；没设 → 后端回
    「还没有设置导出目录」，前端才退回浏览器下载。**只有这一句允许静默降级**，
    别的错误照样抛出来，否则会假装导出成功。
  · 真机实测：保存目录后回执 `导出文件会写到 E:\...\exports254`；
    书架「导出全书…」→ `已保存到 ...\观星残卷（流程测试）_全书.txt`（文件确实在目录里）；
    树里「导出为 Markdown」→ `已保存到 ...\观星残卷（流程测试）_全书蓝图.md`（同一个目录，两处共用）。
  · 目录必须是**绝对路径**（相对路径 400 并说明），空值 = 关掉这条、回到浏览器下载。
  · 红线复核：`POST /api/novels/{id}/export/save` 是**磁盘写**，内容全部来自只读投影与正文，
    四层规划那条唯一写入口一次都没被绕开；测试里专门钉了一条「保存导出前后 chapters 逐字段相同」。
  · 测试：后端新增 5 条（读回/相对路径拒/未设目录 409/全书与单章落盘/文档用自家 label 命名/不写库），
    前端把三条导出用例改成**显式演「没设目录」那一条**并加了一条设置页第三栏的断言。
    后端 208 → **213**、前端 204（条数不变，改了 4 条用例的桩）。
  · 过程里我自己写坏过一次 `PreferencesPage.tsx`（把新组件插进主组件函数体里、还多一个
    提前 return）—— `git checkout` 单文件回滚后重做，教训：**插新组件要插在模块层，不插在组件里**。

### 25.5 备份 + 撤销入口（他这一批的主功能，恢复逻辑他逐条给了）

- 主人：「做备份是为了防备我误删或删除后反悔。因此需要给我一个撤销的入口，可以在设置里做一个页面，
  显示我删除了哪些文件、有哪些需要恢复」+ 三条恢复语义：
  ① 恢复整本书 → 直接恢复到首页；② 恢复单个文件 → 回到书里的原文件位置；
  ③ 删了一本书但只想恢复其中一个文件 → **先提醒是否连整本书一起恢复**，
     选否则把该文件下载到默认目录（与 25.4 同一项）。
- 判据：
  1. 删除前自动快照：SQLite 是单文件，复制一份带时间戳的 `.db`，并记一条**删除日志**
     （删的是什么、什么时候、属于哪本书、在哪个快照里）；
  2. 设置「导出与恢复」这一栏列出删除记录（一行一条，最新在前），每条给「恢复」；
  3. 恢复整本书 → 书回到首页书架，且**不覆盖现有同名书**（先量再定：撞名就拒绝并说明）；
  4. 恢复单个文件 → 走那唯一一条写入口 `PUT /api/novels/{id}/files/{path}` 写回原路径，
     **不开第二条写通路**（这是红线，恢复也不能例外）；
  5. 跨书只恢复一个文件 → 弹窗先问「要连整本书一起恢复吗」；选「只恢复这个文件」→ 写到 25.4 的默认目录；
  6. 备份目录与保留份数有上限（默认 20 份，最旧的自动清），不承诺一键回滚整库；
  7. 全程**不动他库里现有数据**（除 25.6 那条明确授权的），测试用隔离库跑。

### 25.6 删掉 `[TOOL_CALL]` 那条历史脏数据（已授权，不再回来问）

- 主人：「如果没什么影响的话，就给我删了。你不要一直问这个问题了」
- 定位（只读查库，实测）：`chat_message id=20`（novel 5，role=assistant，内容以
  `[TOOL_CALL]\n{tool => "read_file", args => { --path "settin…` 开头）；
  全库其余表（`generation_run`／`review`／`chapter_summary`／`plot_feedback`）**0 命中**。
- 判据：删前先把 `backend/novel_generator.db` 复制一份到备份目录并把文件名报给主人；
  删后 `chat_message` 行数 **31 → 30**、novel 5 少这一条、其余消息与注入清单不变；
  后端全量测试仍全绿（证明没有用例依赖它）；这条决定写进 `DECISIONS §5` 关闭，**以后不再问**。

- **已删（2026-09-06，主人明确授权，此题关闭）**：删前先把整库复制成
  `backend/backups/pre-delete-20260906-153623.db`（**434,176 B**，`*.db` 已在 .gitignore 里，不入库）；
  然后 `DELETE FROM chat_message WHERE id=20`。实测：`chat_message` **31 → 30**、
  全库 `TOOL_CALL` 命中 **0**、novel 5 相邻三条（id 29/30/31）内容与角色不变；
  真机对话面 **12 条气泡**、页面上搜不到 `TOOL_CALL`、0 个错误块；后端 **208 passed** 不变
  （证明没有用例依赖它）。决定与「以后这类已定过的不再回来问」一起记进 `DECISIONS §5`。

- **已做（2026-09-06 真机走通三条恢复语义，四张截图看过；演练痕迹已清干净）**。
  · **快照**：删一本书之前先复制现场 —— 用 SQLite 的 backup API（不是文件复制，
    服务在跑时纯复制会抄到写一半的库）。命名 `deleted-<时间>-<书号>-<书名>.db`，
    留在 `backend/backups/`，**保留最近 20 份**，更旧的自动清。内存库（测试）没有文件可复制，
    返回 None，所以「删除仍然工作」这条不依赖磁盘。
  · **撤销入口**：设置 →「导出与恢复」多一段「删除记录」，一行一条：
    真机 `《恢复演练》 2026-09-06 16:13:35 · 删除前快照 · 424 KB`，动作贴着它自己那一条放。
  · **① 恢复整本书** → 书回到首页书架；再点第二次如实拒绝：真机 `第 7 号书还在书架上，不覆盖`。
  · **② 恢复单个文件** → 走**那唯一一条文档写入口**放回原路径（恢复也不开第二条门）。
  · **③ 跨书只取一个文件** → 书已经没了：点「放回书里」真机报 `这本书已经不在书架上了`；
    点「只取这个文件」弹三选 `这本书已经不在书架上 / 只取这个文件就不动书架 /
    [取消][只取文件][连书一起恢复]`；选「只取文件」→ 真机 `已写到导出目录`，
    落到**与导出同一个目录设置**（他要的简洁性与通用性，界面上没有第二个目录框）。
  · 快照只认备份目录里符合名字格式的文件（`../../etc/passwd`、`novel_generator.db` 一律 400），
    有测试钉住。
  · 真机撞出来两个 bug，都是我自己造的，已修并写进代码注释：
    ① `DETACH` 在有未关闭游标时报 `database snap is locked` → 先把行全部 `fetchall` 进内存再 detach；
    ② `_snapshot_session` 用默认连接池，Session 关了池子还留着那条 sqlite 连接，
      结果**快照文件被自己的进程占着删不掉**、每看一次快照多攒一个池子——正是主人担心的那种增长，
      改成 `NullPool`。
  · 测试：后端新增 5 条（删了就有快照并列出、恢复整本书连正文一起回来且不覆盖、
    单份文档放回原位、书没了只能进目录、假文件名一律拒）；前端新增 1 条走完整三选流程。
    后端 213 → **218**、前端 204 → **205**。
  · 演练痕迹：临时建的《恢复演练》已删、两份 `deleted-*.db` 已清、导出目录已恢复成他原来的空值，
    作品库回到原有 **6 本**；只留下 25.6 那次删脏数据的安全副本 `pre-delete-20260906-153623.db`。

### 25.7 计划：对话 Agent 的工具面（读写 + 联网）

- 主人：「对话 Agent 除了读写这种基本工具，也要具备联网功能。可参考开源项目、Codex 的 Computer Use、
  插件或 MCP，把这些基本的都给它配上。**你可以出一份计划**。」
- 判据：只出计划不动手。计划写进 `WORKSTREAM-PLAN §六`，必须含：
  ① 现在真有什么（grep 取证，不许照抄旧文档）；② 缺什么；③ 参考谁、各自解决哪一段；
  ④ 分几步、每步的完成口径与风险；⑤ 不碰的红线（写只走一条口、Agent 没有直接改稿权、
  联网结果进上下文但不进真源）。

- **已出（2026-09-06）：`WORKSTREAM-PLAN §六`**。取证结果纠正了旧文档的说法——
  读文件、联网、多步循环**三样都已经在了**（`files()` / `read()` / `search()`，
  `DEFAULT_MAX_STEPS = 6`），但联网只到「查中文维基百科前 5 条摘要」这一档
  （端点写死在 `agent_tools.py:22` 附近）。所以计划不是「加一堆工具」，
  而是补四件实事：**抓网页正文、搜索源变成供应商、两段式检索带引用、预算加字数与时间两个闸**，
  最后把工具改成一张注册表。参考对象逐个写明取哪一段：Codex 的预算与停止条件、
  MCP 的声明格式（**不引运行时**）、检索类开源 agent 的两段式；
  **Computer Use 明确不做**（本机没有要点鼠标的第三方程序，只会换来不可复现的失败）。
  三条边界写死：Agent 永远没有写工具、联网结果只进当轮上下文不进库、不做常驻抓取。

---

## 一、未结的界面条目（按主人点名次数排序）

第二十、二十一轮的拍板项已由主人整体授权给我，裁定见 `DECISIONS.md` **D-22**：
对比度三处已修（`--on-accent` / `--on-danger` 一个 token 管整类，四组配色逐元素复量全 0）、
顶栏方框统一成无边框图标钮、删除语义做两条真端点、`required_facts` 保留机械口径并把
契约写到看得见的地方、S0 在隔离临时库跑 `--live`、生图继续明写未启用。

- [ ] **同一份资料两个名字：树里「全本蓝图」，投影 label 与注入清单「全书蓝图」**
      （25.3 导出时撞见的，实测 `TreePane.tsx:71` 写 `全本蓝图`、`documents.py:509/557` 写
      `全书蓝图`；导出的文件名因此叫「全书蓝图.md」，而树上那一行读作「A 全本蓝图」）。
      与刚修掉的「分卷 / 剧情弧」是同一条病（§0.7 条三）。改法：定一个词，两处对齐 +
      断言扫整类（这次别再一条一条点名）。**本轮不做。**
- [ ] **伏笔墙 / 地图面板接上真实数据源**：面板内**零请求**，是未接线，不是没数据（别再重复排查）。
      **2026-09-05 主人明确排到最后：「伏笔墙跟地图的数据接线往后靠，先把其他的做了」**

---

- [ ] **F5 后停在正文面时，选中的是章本身而不是文件**（21.1 顺手记下，本轮故意不做）：章的恢复走工作台 store 的既有路径，把 `chapter` 也塞进 `workbench.stage` 会造出第三个「谁在台子上」的持有者。

## 二、被后端语义卡住的（不要在前端臆造第二条写通路）

- [ ] 树右键「重命名 / 删除」：**仍然未开放**。章号是主键（D-11），删一章/插一章要回答
      「后面的章号要不要顺延」，那是 D-13 未决的产品语义。界面上继续 `disabled` + 原因。
- [ ] `dbg`（id 6）与 `MD探针`（id 3）两个误建作品：端点已就绪（D-23），
      **我没有替主人删** - 他在书架上右键、把书名打一遍即可。

---

## 三、取证脚本（每轮 UI 改动后跑一遍，别靠目测）

放在 `.scratch/`（已 gitignore，改动不入仓库，结论写进文档）：

- `cdp.mjs` → `connect(url)` 返回 `{ evaluate, click, clickPoint, send, shot, close }`；
  无 Chrome，用裸 Edge + CDP（端口 9333）。
- **写 CDP 脚本一律 `-Encoding utf8`**。用 `ascii` 会把脚本里的中文静默变成 `?`，
  于是 `find(n => n.getAttribute("aria-label") === "展开调用记录")` 永远匹配不上，
  报出来的「控件不存在」全是脚本 bug——本会话被它误导三次。
- **命中区审计**：`node .scratch/hit-area-audit.mjs [url] [open-file|appearance]`。
  它**不信 CSS 盒子**（上一轮就是盒子骗的我：`.tree-prefix` 写着 28px，实际被 overflow
  裁掉），而是用 `elementFromPoint` 从控件中心向四向走，量「真点得到」的范围。
  三段输出：`small`（命中 < 24px）、`clipped`（命中框任何一部分落在最近滚动/裁切容器之外）、
  `unreachable`（中心被别的东西盖住）。改完任何一处控件尺寸都跑一遍。
- **对比度审计**：逐元素算合成背景与前景的 WCAG 比值（浅色主题曾一次抓出 13 处不过 AA）。
- **量「主人看到的线」的正确方法**：给定一条视觉接缝，枚举 `elementFromPoint` 沿途
  以及两侧容器**所有子元素**的 `left/right/backgroundColor`，不要只查你怀疑的那个元素有没有 border。
- **改字号之后**：`#root` 上有 zoom，指针像素 ≠ CSS 像素，任何把指针变成坐标或宽度的地方
  必须过 `toCssPx()`；量法是「拖 80px 看栏宽是不是正好走 80px」「右键菜单的左上角与
  点击点 gap 是否为 0」。

---

## 四、门禁（每次推送前必须全绿）

```
cd E:\novel-generator\backend;  .venv\Scripts\python.exe -m pytest -q
cd E:\novel-generator\frontend; npm run test -- --run
# 期望条数只写在 HANDOFF.md 的门禁段（同一数字抄两处，必有一处是错的 - 已错过三次）
cd E:\novel-generator\frontend; npx tsc -b --force --pretty false            # 必须 clean，--force 别信增量
cd E:\novel-generator\frontend; npm run build                                # 必须 clean
cd E:\novel-generator\.scratch; node hit-area-audit.mjs <url>                # 0 small / 0 clipped / 0 unreachable
git -c http.proxy=http://127.0.0.1:7890 push origin main
```

测试要**跟着设计改断言**，不许删。`WorkbenchLayout.test.tsx` 里那条把「按两次方向键」
写成 `300 + 32` 的算式而不是字面量——上次它绑死 312，因为默认值变了而以错误的原因失败。
